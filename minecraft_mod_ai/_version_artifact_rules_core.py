"""Core legacy artifact-rule builder retained during HOST authority migration.

The public `populate_version_artifact_rules` module owns reviewed post-processing
and catalog publication.  This core is migrated leaf-family by leaf-family so
unreviewed deterministic guesses are removed rather than silently preserved.
"""
