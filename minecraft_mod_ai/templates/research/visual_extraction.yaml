id: research/visual_extraction
input:
  visual_evidence:
task:
  extract_observed_visual_requirements:
rules:
  - Separate observed geometry, material, texture, color, animation, and UI facts.
  - Do not infer unseen visual details.
output:
  visual_records:
  evidence_refs:
proof:
  predicate: every visual record is observable in cited visual evidence
