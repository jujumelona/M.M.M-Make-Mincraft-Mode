id: reuse/official_docs
input:
  queries:
  target_cell:
task:
  collect_official_documentation_candidates:
rules:
  - Prefer official platform, loader, mapping, and library documentation.
  - Preserve source locator and target compatibility metadata.
output:
  candidates:
  unresolved_queries:
proof:
  predicate: every candidate is source-identified and its authority type is explicit
