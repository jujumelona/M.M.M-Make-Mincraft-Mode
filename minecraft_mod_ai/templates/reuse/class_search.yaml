id: reuse/class_search
input:
  file_hit:
  class_need:
task:
  find_candidate_classes:
rules:
  - Identify declarations and ownership boundaries without claiming unobserved behavior.
output:
  class_hits:
proof:
  predicate: every hit resolves to a concrete declaration locator
