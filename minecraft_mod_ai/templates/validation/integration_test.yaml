id: validation/integration_test
input:
  integration_tests:
  integrated_project:
task:
  run_integration_tests:
rules:
  - Exercise declared component and feature boundaries.
  - Preserve failure evidence for each boundary.
output:
  passed:
  failures:
proof:
  predicate: passed is true only when every required integration boundary passes
