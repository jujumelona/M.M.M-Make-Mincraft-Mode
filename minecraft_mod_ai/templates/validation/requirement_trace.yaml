id: validation/requirement_trace
input:
  requirements:
  implementation_receipts:
  test_receipts:
task:
  trace_requirements_to_proof:
rules:
  - Every mandatory requirement needs implementation and verification evidence.
output:
  traces:
  unproven_requirements:
proof:
  predicate: no mandatory requirement lacks an implementation and verification trace
