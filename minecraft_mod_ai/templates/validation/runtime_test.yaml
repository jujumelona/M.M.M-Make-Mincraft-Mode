id: validation/runtime_test
input:
  runtime_scenarios:
  target_runtime:
task:
  execute_runtime_scenarios:
rules:
  - Run only after compile and static prerequisites pass.
  - Capture observable runtime results, not self-reported success.
output:
  passed:
  observations:
  failures:
proof:
  predicate: passed is true only when required observable runtime outcomes are recorded
