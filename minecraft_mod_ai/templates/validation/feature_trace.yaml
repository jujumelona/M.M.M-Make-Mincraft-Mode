id: validation/feature_trace
input:
  atomic_features:
  artifact_receipts:
  test_receipts:
task:
  trace_features_to_artifacts_and_tests:
rules:
  - Preserve atomic feature identity through implementation and tests.
output:
  traces:
  unproven_features:
proof:
  predicate: every atomic feature has artifact and test proof
