id: validation/gameplay_test
input:
  atomic_feature_acceptance:
  runtime_observations:
task:
  verify_gameplay_acceptance:
rules:
  - Check success, rejection, boundaries, and state transitions required by the feature.
  - Do not substitute compile success for gameplay proof.
output:
  passed:
  failed_acceptance:
proof:
  predicate: passed is true only when every required gameplay acceptance condition is observed
