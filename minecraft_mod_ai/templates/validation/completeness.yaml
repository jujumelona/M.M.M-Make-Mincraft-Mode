id: validation/completeness
input:
  requirement_trace:
  feature_trace:
  resource_trace:
  validation_receipts:
task:
  calculate_completion_state:
rules:
  - Completion is host-calculated from required proof receipts.
  - Any blocked, failed, missing, or unresolved mandatory proof prevents completion.
output:
  complete:
  blockers:
proof:
  predicate: complete is true only when every mandatory proof receipt passes
