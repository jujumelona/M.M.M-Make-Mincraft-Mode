id: design/combat_role
slot_id: combat_role
task: "Define the combat mechanic or weapon interaction, or none if non-combat."
rules:
  - "Use only explicit requirements and supplied context; do not invent mechanics."
record_schema:
  type: object
  properties:
    combat_role:
      type: string
      maxLength: 256
  required:
    - combat_role
  additionalProperties: false
