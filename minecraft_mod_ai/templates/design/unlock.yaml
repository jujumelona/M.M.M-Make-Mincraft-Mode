id: design/unlock
slot_id: unlock
task: "Define the explicit persistent milestone or system unlocked by the progression reward."
rules:
  - "Use only explicit requirements and supplied context; do not invent milestones, systems, rewards, or capabilities."
record_schema:
  type: object
  properties:
    unlock:
      type: string
      minLength: 1
      maxLength: 256
  required:
    - unlock
  additionalProperties: false
