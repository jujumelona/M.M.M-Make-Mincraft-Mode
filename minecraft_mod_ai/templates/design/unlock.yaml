id: design/unlock
slot_id: unlock
task: "Define the persistent milestone or system unlocked upon reaching the reward."
rules:
  - "Must cite concrete Minecraft interaction mechanics."
  - "Must stay strictly under 256 characters."
record_schema:
  type: object
  properties:
    unlock:
      type: string
      maxLength: 256
  required:
    - unlock
  additionalProperties: false
