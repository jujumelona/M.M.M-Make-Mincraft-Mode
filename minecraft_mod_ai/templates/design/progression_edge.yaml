id: design/progression_edge
slot_id: progression_edge
task: "Define the trigger or requirement linking progression nodes."
rules:
  - "Must cite concrete Minecraft interaction mechanics."
  - "Must stay strictly under 256 characters."
record_schema:
  type: object
  properties:
    progression_edge:
      type: string
      maxLength: 256
  required:
    - progression_edge
  additionalProperties: false
