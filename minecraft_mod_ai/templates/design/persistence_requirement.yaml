id: design/persistence_requirement
slot_id: persistence_requirement
task: "Define custom state that must persist across saves or sessions."
rules:
  - "Use only explicit requirements and supplied context; do not invent saved state, capabilities, or statistics."
record_schema:
  type: object
  properties:
    persistence_requirement:
      type: string
      maxLength: 256
  required:
    - persistence_requirement
  additionalProperties: false
