id: design/persistence_requirement
slot_id: persistence_requirement
task: "Define custom data, capability state, or player statistics saved to disk."
rules:
  - "Must cite concrete Minecraft interaction mechanics."
  - "Must stay strictly under 256 characters."
record_schema:
  type: object
  properties:
    persistence_requirement:
      type: string
      maxLength: 256
  required:
    - persistence_requirement
  additionalProperties: false
