id: design/core_loop
slot_id: core_loop
task: "Define the single primary gameplay loop for this Minecraft mod in one sentence."
rules:
  - "Use only explicit requirements and supplied context; do not invent loop steps."
record_schema:
  type: object
  properties:
    core_loop:
      type: string
      maxLength: 256
  required:
    - core_loop
  additionalProperties: false
