id: design/exploration_target
slot_id: exploration_target
task: "Define key biomes, structures, or dimensions to explore."
rules:
  - "Use only explicit requirements and supplied context; do not invent locations or mechanics."
record_schema:
  type: object
  properties:
    exploration_target:
      type: string
      minLength: 1
      maxLength: 256
  required:
    - exploration_target
  additionalProperties: false
