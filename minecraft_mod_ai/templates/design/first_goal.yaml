id: design/first_goal
slot_id: first_goal
task: "Define the initial immediate player objective upon starting the mod."
rules:
  - "Must cite concrete Minecraft interaction mechanics."
  - "Must stay strictly under 256 characters."
record_schema:
  type: object
  properties:
    first_goal:
      type: string
      maxLength: 256
  required:
    - first_goal
  additionalProperties: false
