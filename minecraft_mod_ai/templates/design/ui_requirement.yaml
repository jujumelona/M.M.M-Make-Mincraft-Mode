id: design/ui_requirement
slot_id: ui_requirement
task: "Define custom screens, container menus, or HUD overlays required."
rules:
  - "Must cite concrete Minecraft interaction mechanics."
  - "Must stay strictly under 256 characters."
record_schema:
  type: object
  properties:
    ui_requirement:
      type: string
      maxLength: 256
  required:
    - ui_requirement
  additionalProperties: false
