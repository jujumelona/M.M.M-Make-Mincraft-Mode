id: design/economy_sink
slot_id: economy_sink
task: "Define currency sinks, merchant offerings, or economic investments."
rules:
  - "Use only explicit requirements and supplied context; do not invent currencies, merchants, or costs."
record_schema:
  type: object
  properties:
    economy_sink:
      type: string
      maxLength: 256
  required:
    - economy_sink
  additionalProperties: false
