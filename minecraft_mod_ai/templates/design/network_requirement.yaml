id: design/network_requirement
slot_id: network_requirement
task: "Define client-server synchronization required by the requested behavior."
rules:
  - "Use only explicit requirements and supplied context; do not invent packets, synchronized state, HUD state, or effects."
record_schema:
  type: object
  properties:
    network_requirement:
      type: string
      minLength: 1
      maxLength: 256
  required:
    - network_requirement
  additionalProperties: false
