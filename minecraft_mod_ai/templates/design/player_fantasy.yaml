id: design/player_fantasy
slot_id: player_fantasy
task: "Define the core role and fantasy experienced by the player."
rules:
  - "Must cite concrete Minecraft interaction mechanics."
  - "Must stay strictly under 256 characters."
record_schema:
  type: object
  properties:
    player_fantasy:
      type: string
      maxLength: 256
  required:
    - player_fantasy
  additionalProperties: false
