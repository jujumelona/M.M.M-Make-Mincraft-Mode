id: design/core_action
slot_id: core_action
task: "Define the primary mechanical verb and interaction performed by the player."
rules:
  - "Use only explicit requirements and supplied context; do not invent mechanics."
record_schema:
  type: object
  properties:
    core_action:
      type: string
      maxLength: 256
  required:
    - core_action
  additionalProperties: false
