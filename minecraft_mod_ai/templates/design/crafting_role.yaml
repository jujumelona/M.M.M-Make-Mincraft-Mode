id: design/crafting_role
slot_id: crafting_role
task: "Define recipe stations, crafting steps, or assembly mechanics."
rules:
  - "Use only explicit requirements and supplied context; do not invent recipes, stations, or mechanics."
record_schema:
  type: object
  properties:
    crafting_role:
      type: string
      maxLength: 256
  required:
    - crafting_role
  additionalProperties: false
