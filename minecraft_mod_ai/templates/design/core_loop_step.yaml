id: design/core_loop_step
slot_id: core_loop_step
task: "Define one sequential step in the cyclical gameplay loop."
rules:
  - "Use only explicit requirements and supplied context; do not invent steps or mechanics."
  - "Return one loop step only."
record_schema:
  type: object
  properties:
    core_loop_step:
      type: string
      maxLength: 256
  required:
    - core_loop_step
  additionalProperties: false
