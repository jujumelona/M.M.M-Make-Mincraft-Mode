id: design/texture_requirement
slot_id: texture_requirement
task: "Define the explicit texture assets required by the requested content."
rules:
  - "Use only explicit requirements and supplied context; do not invent items, blocks, entities, or visual assets."
record_schema:
  type: object
  properties:
    texture_requirement:
      type: string
      minLength: 1
      maxLength: 256
  required:
    - texture_requirement
  additionalProperties: false
