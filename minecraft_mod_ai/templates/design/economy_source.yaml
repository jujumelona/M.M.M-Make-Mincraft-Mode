id: design/economy_source
slot_id: economy_source
task: "Define trading, bartering, or currency acquisition methods."
rules:
  - "Use only explicit requirements and supplied context; do not invent currencies, trades, or acquisition methods."
record_schema:
  type: object
  properties:
    economy_source:
      type: string
      maxLength: 256
  required:
    - economy_source
  additionalProperties: false
