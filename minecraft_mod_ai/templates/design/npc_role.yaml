id: design/npc_role
slot_id: npc_role
task: "Define non-player character interactions, dialogues, or companion behaviors."
rules:
  - "Must cite concrete Minecraft interaction mechanics."
  - "Must stay strictly under 256 characters."
record_schema:
  type: object
  properties:
    npc_role:
      type: string
      maxLength: 256
  required:
    - npc_role
  additionalProperties: false
