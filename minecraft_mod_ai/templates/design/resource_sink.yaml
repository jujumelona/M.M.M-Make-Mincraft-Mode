id: design/resource_sink
slot_id: resource_sink
task: "Define how gathered resources are spent, consumed, or maintained."
rules:
  - "Must cite concrete Minecraft interaction mechanics."
  - "Must stay strictly under 256 characters."
record_schema:
  type: object
  properties:
    resource_sink:
      type: string
      maxLength: 256
  required:
    - resource_sink
  additionalProperties: false
