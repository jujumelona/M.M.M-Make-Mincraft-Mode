id: design/resource_sink
slot_id: resource_sink
task: "Define how explicitly required resources are spent, consumed, or maintained."
rules:
  - "Use only explicit requirements and supplied context; do not invent resources, costs, upkeep, or mechanics."
record_schema:
  type: object
  properties:
    resource_sink:
      type: string
      maxLength: 256
  required:
    - resource_sink
  additionalProperties: false
