id: design/resource_source
slot_id: resource_source
task: "Define where and how foundational resources are gathered in the world."
rules:
  - "Must cite concrete Minecraft interaction mechanics."
  - "Must stay strictly under 256 characters."
record_schema:
  type: object
  properties:
    resource_source:
      type: string
      maxLength: 256
  required:
    - resource_source
  additionalProperties: false
