id: asset/texture_plan
execution: host
input:
  texture_requirement:
  visual_evidence:
output:
  texture_contract:
  unresolved_properties:
proof:
  predicate: every fixed texture property is evidence-backed or explicitly authored
