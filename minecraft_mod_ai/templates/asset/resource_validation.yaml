id: asset/resource_validation
execution: host
input:
  generated_resource:
  resource_contract:
output:
  valid:
  failed_checks:
proof:
  predicate: valid is true only when every declared resource check passes
