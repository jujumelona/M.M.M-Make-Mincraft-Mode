id: minecraft/texture/dimensions
task: Implement only the dimensions responsibility of the supplied texture artifact.
execution_mode: model
side:
- client
inputs:
- name: texture_identity
  type: identifier
  required: true
- name: texture_dimensions_input
  type: specification
  required: true
outputs:
- name: texture_dimensions_receipt
  type: receipt
- name: texture_dimensions_artifact
  type: code_fragment
host_requirements:
  capabilities:
  - REGISTER_ITEM
  symbols:
  - register_item
  schemas: []
implementation_key: fabric/texture/dimensions
validators:
- java_parse
- client_side_only
context_projection:
  max_bytes: 4096
  include:
  - mod_id
  - registry_path
  - package_path
  - texture_properties
repair:
  scope:
  - minecraft/texture/dimensions
  writable_anchors:
  - /* MMM:texture_dimensions */
consumes:
- artifact_requirement
- target_cell
- verified_bindings
- owned_anchor
- prerequisite_receipts
provides:
- dimensions
anchor_kinds:
- resource
- test
rules:
- Implement dimensions responsibility for texture strictly within declared TEXTURE_ASSET
  capability.
- Target side must remain client; reject cross-side symbol leakage.
- Fail closed if required texture prerequisite receipt is missing; never invent defaults.
- Output must satisfy java_parse validator before completion.
postconditions:
- texture dimensions matches host version contract
- texture dimensions receipts emitted with verified context
