id: minecraft/effect/removal
task: Implement only the removal responsibility of the supplied effect artifact.
execution_mode: model
side:
- common
inputs:
- name: effect_identity
  type: identifier
  required: true
- name: effect_removal_input
  type: specification
  required: true
outputs:
- name: effect_removal_receipt
  type: receipt
- name: effect_removal_artifact
  type: code_fragment
host_requirements:
  capabilities:
  - STATUS_EFFECT
  symbols:
  - StatusEffect
  schemas: []
implementation_key: fabric/effect/removal
validators:
- java_parse
context_projection:
  max_bytes: 4096
  include:
  - mod_id
  - registry_path
  - package_path
  - effect_properties
repair:
  scope:
  - minecraft/effect/removal
  writable_anchors:
  - /* MMM:effect_removal */
consumes:
- artifact_requirement
- target_cell
- verified_bindings
- owned_anchor
- prerequisite_receipts
provides:
- removal
anchor_kinds:
- symbol
- test
rules:
- Implement removal responsibility for effect strictly within declared STATUS_EFFECT
  capability.
- Target side must remain common; reject cross-side symbol leakage.
- Fail closed if required effect prerequisite receipt is missing; never invent defaults.
- Output must satisfy java_parse validator before completion.
postconditions:
- effect removal matches host version contract
- effect removal receipts emitted with verified context
