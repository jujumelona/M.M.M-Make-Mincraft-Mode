id: minecraft/item/texture
task: Implement only the texture responsibility of the supplied item artifact.
execution_mode: model
side:
- client
inputs:
- name: item_identity
  type: identifier
  required: true
outputs:
- name: item_texture_receipt
  type: receipt
host_requirements:
  capabilities:
  - REGISTER_ITEM
implementation_key: fabric/item/texture
validators:
- java_parse
- client_side_only
context_projection:
  max_bytes: 4096
  include:
  - mod_id
  - registry_path
  - package_path
repair:
  scope:
  - minecraft/item/texture
  writable_anchors:
  - /* MMM:item_texture */
consumes:
- artifact_requirement
- target_cell
- verified_bindings
- owned_anchor
- prerequisite_receipts
provides:
- texture
anchor_kinds:
- symbol
- test
rules:
- Implement texture responsibility for item strictly within declared REGISTER_ITEM
  capability.
- Target side must remain client; reject cross-side symbol leakage.
- Fail closed if required item prerequisite receipt is missing; never invent defaults.
- Output must satisfy java_parse validator before completion.
