id: minecraft/loot/datagen
task: Implement only the datagen responsibility of the supplied loot artifact.
execution_mode: deterministic
side:
- common
inputs:
- name: loot_identity
  type: identifier
  required: true
- name: loot_datagen_input
  type: specification
  required: true
outputs:
- name: loot_datagen_receipt
  type: receipt
- name: loot_datagen_artifact
  type: code_fragment
host_requirements:
  capabilities:
  - LOOT_TABLE
  symbols: []
  schemas:
  - fabric/loot/block_drop
implementation_key: fabric/loot/datagen
validators:
- json_schema
context_projection:
  max_bytes: 4096
  include:
  - mod_id
  - registry_path
  - package_path
  - loot_properties
repair:
  scope:
  - minecraft/loot/datagen
  writable_anchors:
  - /* MMM:loot_datagen */
consumes:
- artifact_requirement
- target_cell
- verified_bindings
- owned_anchor
- prerequisite_receipts
provides:
- datagen
anchor_kinds:
- resource
- test
rules:
- Implement datagen responsibility for loot strictly within declared LOOT_TABLE capability.
- Target side must remain common; reject cross-side symbol leakage.
- Fail closed if required loot prerequisite receipt is missing; never invent defaults.
- Output must satisfy json_schema validator before completion.
postconditions:
- loot datagen matches host version contract
- loot datagen receipts emitted with verified context
