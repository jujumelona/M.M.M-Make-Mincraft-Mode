id: minecraft/loot/entry
task: Implement only the entry responsibility of the supplied loot artifact.
execution_mode: deterministic
side:
- common
inputs:
- name: loot_identity
  type: identifier
  required: true
outputs:
- name: loot_json
  type: resource_file
host_requirements:
  capabilities:
  - LOOT_TABLE
implementation_key: fabric/loot/entry
validators:
- json_schema
context_projection:
  max_bytes: 4096
  include:
  - mod_id
  - registry_path
  - package_path
repair:
  scope:
  - minecraft/loot/entry
  writable_anchors:
  - /* MMM:loot_entry */
consumes:
- artifact_requirement
- target_cell
- verified_bindings
- owned_anchor
- prerequisite_receipts
provides:
- entry
anchor_kinds:
- resource
- test
rules:
- Implement entry responsibility for loot strictly within declared LOOT_TABLE capability.
- Target side must remain common; reject cross-side symbol leakage.
- Fail closed if required loot prerequisite receipt is missing; never invent defaults.
- Output must satisfy json_schema validator before completion.
