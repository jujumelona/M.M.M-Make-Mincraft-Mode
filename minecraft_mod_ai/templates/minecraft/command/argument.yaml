id: minecraft/command/argument
task: Implement only the argument responsibility of the supplied command artifact.
execution_mode: model
side:
- common
inputs:
- name: command_identity
  type: identifier
  required: true
- name: command_argument_input
  type: specification
  required: true
outputs:
- name: command_argument_receipt
  type: receipt
- name: command_argument_artifact
  type: code_fragment
host_requirements:
  capabilities:
  - REGISTER_ITEM
  symbols:
  - register_item
  schemas: []
implementation_key: fabric/command/argument
validators:
- java_parse
context_projection:
  max_bytes: 4096
  include:
  - mod_id
  - registry_path
  - package_path
  - command_properties
repair:
  scope:
  - minecraft/command/argument
  writable_anchors:
  - /* MMM:command_argument */
consumes:
- artifact_requirement
- target_cell
- verified_bindings
- owned_anchor
- prerequisite_receipts
provides:
- argument
anchor_kinds:
- symbol
- test
rules:
- Implement argument responsibility for command strictly within declared COMMAND_REGISTRATION
  capability.
- Target side must remain server; reject cross-side symbol leakage.
- Fail closed if required command prerequisite receipt is missing; never invent defaults.
- Output must satisfy java_parse validator before completion.
postconditions:
- command argument matches host version contract
- command argument receipts emitted with verified context
