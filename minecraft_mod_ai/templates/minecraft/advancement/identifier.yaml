id: minecraft/advancement/identifier
task: Implement only the identifier responsibility of the supplied advancement artifact.
execution_mode: model
side:
- common
inputs:
- name: advancement_identity
  type: identifier
  required: true
outputs:
- name: advancement_identifier_receipt
  type: receipt
host_requirements:
  capabilities:
  - ADVANCEMENT
implementation_key: fabric/advancement/identifier
validators:
- java_parse
context_projection:
  max_bytes: 4096
  include:
  - mod_id
  - registry_path
  - package_path
repair:
  scope:
  - minecraft/advancement/identifier
  writable_anchors:
  - /* MMM:advancement_identifier */
consumes:
- artifact_requirement
- target_cell
- verified_bindings
- owned_anchor
- prerequisite_receipts
provides:
- identifier
anchor_kinds:
- resource
- test
rules:
- Implement identifier responsibility for advancement strictly within declared ADVANCEMENT
  capability.
- Target side must remain common; reject cross-side symbol leakage.
- Fail closed if required advancement prerequisite receipt is missing; never invent
  defaults.
- Output must satisfy java_parse validator before completion.
