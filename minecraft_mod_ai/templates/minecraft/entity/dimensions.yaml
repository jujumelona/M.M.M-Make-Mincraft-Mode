id: minecraft/entity/dimensions
task: Implement only the dimensions responsibility of the supplied entity artifact.
execution_mode: model
side:
- common
inputs:
- name: entity_identity
  type: identifier
  required: true
- name: entity_dimensions_input
  type: specification
  required: true
outputs:
- name: entity_dimensions_receipt
  type: receipt
- name: entity_dimensions_artifact
  type: code_fragment
host_requirements:
  capabilities:
  - ENTITY_EXISTS
  symbols:
  - EntityType
  schemas: []
implementation_key: fabric/entity/dimensions
validators:
- java_parse
context_projection:
  max_bytes: 4096
  include:
  - mod_id
  - registry_path
  - package_path
  - entity_properties
repair:
  scope:
  - minecraft/entity/dimensions
  writable_anchors:
  - /* MMM:entity_dimensions */
consumes:
- artifact_requirement
- target_cell
- verified_bindings
- owned_anchor
- prerequisite_receipts
provides:
- dimensions
anchor_kinds:
- symbol
- test
rules:
- Implement dimensions responsibility for entity strictly within declared REGISTER_ENTITY
  capability.
- Target side must remain common; reject cross-side symbol leakage.
- Fail closed if required entity prerequisite receipt is missing; never invent defaults.
- Output must satisfy java_parse validator before completion.
postconditions:
- entity dimensions matches host version contract
- entity dimensions receipts emitted with verified context
