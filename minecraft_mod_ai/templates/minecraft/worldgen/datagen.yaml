id: minecraft/worldgen/datagen
task: Implement only the datagen responsibility of the supplied worldgen artifact.
execution_mode: deterministic
side:
- common
inputs:
- name: worldgen_identity
  type: identifier
  required: true
- name: worldgen_datagen_input
  type: specification
  required: true
outputs:
- name: worldgen_datagen_receipt
  type: receipt
- name: worldgen_datagen_artifact
  type: code_fragment
host_requirements:
  capabilities:
  - WORLDGEN_FEATURE
  symbols: []
  schemas: []
implementation_key: fabric/worldgen/datagen
validators:
- json_schema
context_projection:
  max_bytes: 4096
  include:
  - mod_id
  - registry_path
  - package_path
  - worldgen_properties
repair:
  scope:
  - minecraft/worldgen/datagen
  writable_anchors:
  - /* MMM:worldgen_datagen */
consumes:
- artifact_requirement
- target_cell
- verified_bindings
- owned_anchor
- prerequisite_receipts
provides:
- datagen
anchor_kinds:
- symbol
- test
rules:
- Implement datagen responsibility for worldgen strictly within declared WORLDGEN_FEATURE
  capability.
- Target side must remain server; reject cross-side symbol leakage.
- Fail closed if required worldgen prerequisite receipt is missing; never invent defaults.
- Output must satisfy json_schema validator before completion.
postconditions:
- worldgen datagen matches host version contract
- worldgen datagen receipts emitted with verified context
