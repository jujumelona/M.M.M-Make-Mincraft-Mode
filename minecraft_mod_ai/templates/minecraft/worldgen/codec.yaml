id: minecraft/worldgen/codec
task: Implement only the codec responsibility of the supplied worldgen artifact.
execution_mode: model
side:
- common
inputs:
- name: worldgen_identity
  type: identifier
  required: true
- name: worldgen_codec_input
  type: specification
  required: true
outputs:
- name: worldgen_codec_receipt
  type: receipt
- name: worldgen_codec_artifact
  type: code_fragment
host_requirements:
  capabilities:
  - WORLDGEN_FEATURE
  symbols: []
  schemas: []
implementation_key: fabric/worldgen/codec
validators:
- java_parse
context_projection:
  max_bytes: 4096
  include:
  - mod_id
  - registry_path
  - package_path
  - worldgen_properties
repair:
  scope:
  - minecraft/worldgen/codec
  writable_anchors:
  - /* MMM:worldgen_codec */
consumes:
- artifact_requirement
- target_cell
- verified_bindings
- owned_anchor
- prerequisite_receipts
provides:
- codec
anchor_kinds:
- symbol
- test
rules:
- Implement codec responsibility for worldgen strictly within declared WORLDGEN_FEATURE
  capability.
- Target side must remain server; reject cross-side symbol leakage.
- Fail closed if required worldgen prerequisite receipt is missing; never invent defaults.
- Output must satisfy java_parse validator before completion.
postconditions:
- worldgen codec matches host version contract
- worldgen codec receipts emitted with verified context
