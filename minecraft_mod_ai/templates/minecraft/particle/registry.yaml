id: minecraft/particle/registry
task: Implement only the registry responsibility of the supplied particle artifact.
execution_mode: model
side:
- common
inputs:
- name: particle_identity
  type: identifier
  required: true
- name: particle_registry_input
  type: specification
  required: true
outputs:
- name: particle_registry_receipt
  type: receipt
- name: particle_registry_artifact
  type: code_fragment
host_requirements:
  capabilities:
  - PARTICLE_TYPE
  symbols:
  - ParticleType
  schemas: []
implementation_key: fabric/particle/registry
validators:
- java_parse
context_projection:
  max_bytes: 4096
  include:
  - mod_id
  - registry_path
  - package_path
  - particle_properties
repair:
  scope:
  - minecraft/particle/registry
  writable_anchors:
  - /* MMM:particle_registry */
consumes:
- artifact_requirement
- target_cell
- verified_bindings
- owned_anchor
- prerequisite_receipts
provides:
- registry
anchor_kinds:
- registry_id
- symbol
- test
rules:
- Implement registry responsibility for particle strictly within declared PARTICLE_TYPE
  capability.
- Target side must remain client; reject cross-side symbol leakage.
- Fail closed if required particle prerequisite receipt is missing; never invent defaults.
- Output must satisfy java_parse validator before completion.
postconditions:
- particle registry matches host version contract
- particle registry receipts emitted with verified context
