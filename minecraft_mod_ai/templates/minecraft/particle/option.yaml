id: minecraft/particle/option
task: Implement only the option responsibility of the supplied particle artifact.
execution_mode: model
side:
- common
inputs:
- name: particle_identity
  type: identifier
  required: true
- name: particle_option_input
  type: specification
  required: true
outputs:
- name: particle_option_receipt
  type: receipt
- name: particle_option_artifact
  type: code_fragment
host_requirements:
  capabilities:
  - PARTICLE_TYPE
  symbols:
  - ParticleType
  schemas: []
implementation_key: fabric/particle/option
validators:
- java_parse
context_projection:
  max_bytes: 4096
  include:
  - mod_id
  - registry_path
  - package_path
  - particle_properties
repair:
  scope:
  - minecraft/particle/option
  writable_anchors:
  - /* MMM:particle_option */
consumes:
- artifact_requirement
- target_cell
- verified_bindings
- owned_anchor
- prerequisite_receipts
provides:
- option
anchor_kinds:
- symbol
- test
rules:
- Implement option responsibility for particle strictly within declared PARTICLE_TYPE
  capability.
- Target side must remain client; reject cross-side symbol leakage.
- Fail closed if required particle prerequisite receipt is missing; never invent defaults.
- Output must satisfy java_parse validator before completion.
postconditions:
- particle option matches host version contract
- particle option receipts emitted with verified context
