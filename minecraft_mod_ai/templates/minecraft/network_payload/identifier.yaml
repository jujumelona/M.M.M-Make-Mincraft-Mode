id: minecraft/network_payload/identifier
task: Implement only the identifier responsibility of the supplied network payload
  artifact.
execution_mode: model
side:
- common
inputs:
- name: network_payload_identity
  type: identifier
  required: true
outputs:
- name: network_payload_identifier_receipt
  type: receipt
host_requirements:
  capabilities:
  - NETWORKING
  symbols:
  - payload_type_register
implementation_key: fabric/network_payload/identifier
validators:
- java_parse
context_projection:
  max_bytes: 4096
  include:
  - mod_id
  - registry_path
  - package_path
repair:
  scope:
  - minecraft/network_payload/identifier
  writable_anchors:
  - /* MMM:network_payload_identifier */
consumes:
- artifact_requirement
- target_cell
- verified_bindings
- owned_anchor
- prerequisite_receipts
provides:
- identifier
anchor_kinds:
- symbol
- test
rules:
- Implement identifier responsibility for network_payload strictly within declared
  NETWORKING capability.
- Target side must remain common; reject cross-side symbol leakage.
- Fail closed if required network_payload prerequisite receipt is missing; never invent
  defaults.
- Output must satisfy java_parse validator before completion.
