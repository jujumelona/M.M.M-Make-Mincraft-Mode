id: minecraft/saved_data/codec
task: Implement only the codec responsibility of the supplied saved data artifact.
execution_mode: model
side:
- server
inputs:
- name: saved_data_identity
  type: identifier
  required: true
outputs:
- name: saved_data_codec_receipt
  type: receipt
host_requirements:
  capabilities:
  - PERSISTENT_STATE
implementation_key: fabric/saved_data/codec
validators:
- java_parse
context_projection:
  max_bytes: 4096
  include:
  - mod_id
  - registry_path
  - package_path
repair:
  scope:
  - minecraft/saved_data/codec
  writable_anchors:
  - /* MMM:saved_data_codec */
consumes:
- artifact_requirement
- target_cell
- verified_bindings
- owned_anchor
- prerequisite_receipts
provides:
- codec
anchor_kinds:
- symbol
- test
rules:
- Implement codec responsibility for saved_data strictly within declared PERSISTENT_STATE
  capability.
- Target side must remain server; reject cross-side symbol leakage.
- Fail closed if required saved_data prerequisite receipt is missing; never invent
  defaults.
- Output must satisfy java_parse validator before completion.
