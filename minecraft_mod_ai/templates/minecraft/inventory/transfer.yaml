id: minecraft/inventory/transfer
task: Implement only the transfer responsibility of the supplied inventory artifact.
execution_mode: model
side:
- common
inputs:
- name: inventory_identity
  type: identifier
  required: true
- name: inventory_transfer_input
  type: specification
  required: true
outputs:
- name: inventory_transfer_receipt
  type: receipt
- name: inventory_transfer_artifact
  type: code_fragment
host_requirements:
  capabilities:
  - REGISTER_ITEM
  symbols:
  - register_item
  schemas: []
implementation_key: fabric/inventory/transfer
validators:
- java_parse
context_projection:
  max_bytes: 4096
  include:
  - mod_id
  - registry_path
  - package_path
  - inventory_properties
repair:
  scope:
  - minecraft/inventory/transfer
  writable_anchors:
  - /* MMM:inventory_transfer */
consumes:
- artifact_requirement
- target_cell
- verified_bindings
- owned_anchor
- prerequisite_receipts
provides:
- transfer
anchor_kinds:
- symbol
- test
rules:
- Implement transfer responsibility for inventory strictly within declared INVENTORY_CAPABILITY
  capability.
- Target side must remain common; reject cross-side symbol leakage.
- Fail closed if required inventory prerequisite receipt is missing; never invent
  defaults.
- Output must satisfy java_parse validator before completion.
postconditions:
- inventory transfer matches host version contract
- inventory transfer receipts emitted with verified context
