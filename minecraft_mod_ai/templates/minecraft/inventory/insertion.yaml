id: minecraft/inventory/insertion
task: Implement only the insertion responsibility of the supplied inventory artifact.
execution_mode: model
side:
- common
inputs:
- name: inventory_identity
  type: identifier
  required: true
outputs:
- name: inventory_insertion_receipt
  type: receipt
host_requirements:
  capabilities:
  - INVENTORY_CAPABILITY
implementation_key: fabric/inventory/insertion
validators:
- java_parse
context_projection:
  max_bytes: 4096
  include:
  - mod_id
  - registry_path
  - package_path
repair:
  scope:
  - minecraft/inventory/insertion
  writable_anchors:
  - /* MMM:inventory_insertion */
consumes:
- artifact_requirement
- target_cell
- verified_bindings
- owned_anchor
- prerequisite_receipts
provides:
- insertion
anchor_kinds:
- symbol
- test
rules:
- Implement insertion responsibility for inventory strictly within declared INVENTORY_CAPABILITY
  capability.
- Target side must remain common; reject cross-side symbol leakage.
- Fail closed if required inventory prerequisite receipt is missing; never invent
  defaults.
- Output must satisfy java_parse validator before completion.
