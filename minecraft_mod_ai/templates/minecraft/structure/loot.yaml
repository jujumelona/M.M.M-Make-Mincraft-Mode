id: minecraft/structure/loot
task: Implement only the loot responsibility of the supplied structure artifact.
execution_mode: model
side:
- server
inputs:
- name: structure_identity
  type: identifier
  required: true
outputs:
- name: structure_loot_receipt
  type: receipt
host_requirements:
  capabilities:
  - WORLDGEN_STRUCTURE
implementation_key: fabric/structure/loot
validators:
- java_parse
context_projection:
  max_bytes: 4096
  include:
  - mod_id
  - registry_path
  - package_path
repair:
  scope:
  - minecraft/structure/loot
  writable_anchors:
  - /* MMM:structure_loot */
consumes:
- artifact_requirement
- target_cell
- verified_bindings
- owned_anchor
- prerequisite_receipts
provides:
- loot
anchor_kinds:
- symbol
- test
rules:
- Implement loot responsibility for structure strictly within declared WORLDGEN_STRUCTURE
  capability.
- Target side must remain server; reject cross-side symbol leakage.
- Fail closed if required structure prerequisite receipt is missing; never invent
  defaults.
- Output must satisfy java_parse validator before completion.
