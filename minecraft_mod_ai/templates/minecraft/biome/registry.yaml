id: minecraft/biome/registry
task: Implement only the registry responsibility of the supplied biome artifact.
execution_mode: model
side:
- server
inputs:
- name: biome_identity
  type: identifier
  required: true
outputs:
- name: biome_registry_id
  type: string
- name: biome_java_symbol
  type: string
host_requirements:
  capabilities:
  - BIOME
implementation_key: fabric/biome/registry
validators:
- java_parse
- registry_identifier_unique
context_projection:
  max_bytes: 4096
  include:
  - mod_id
  - registry_path
  - package_path
  - java_constant
repair:
  scope:
  - minecraft/biome/registry
  writable_anchors:
  - /* MMM:biome_registry */
consumes:
- artifact_requirement
- target_cell
- verified_bindings
- owned_anchor
- prerequisite_receipts
provides:
- registry
anchor_kinds:
- registry_id
- symbol
- test
rules:
- Implement registry responsibility for biome strictly within declared BIOME capability.
- Target side must remain server; reject cross-side symbol leakage.
- Fail closed if required biome prerequisite receipt is missing; never invent defaults.
- Output must satisfy java_parse validator before completion.
