id: minecraft/biome/generation
task: Implement only the generation responsibility of the supplied biome artifact.
execution_mode: model
side:
- server
inputs:
- name: biome_identity
  type: identifier
  required: true
outputs:
- name: biome_generation_receipt
  type: receipt
host_requirements:
  capabilities:
  - BIOME
implementation_key: fabric/biome/generation
validators:
- java_parse
context_projection:
  max_bytes: 4096
  include:
  - mod_id
  - registry_path
  - package_path
repair:
  scope:
  - minecraft/biome/generation
  writable_anchors:
  - /* MMM:biome_generation */
consumes:
- artifact_requirement
- target_cell
- verified_bindings
- owned_anchor
- prerequisite_receipts
provides:
- generation
anchor_kinds:
- symbol
- test
rules:
- Implement generation responsibility for biome strictly within declared BIOME capability.
- Target side must remain server; reject cross-side symbol leakage.
- Fail closed if required biome prerequisite receipt is missing; never invent defaults.
- Output must satisfy java_parse validator before completion.
