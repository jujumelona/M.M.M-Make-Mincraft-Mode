id: minecraft/biome/tags
task: Implement only the tags responsibility of the supplied biome artifact.
execution_mode: model
side:
- common
inputs:
- name: biome_identity
  type: identifier
  required: true
- name: biome_tags_input
  type: specification
  required: true
outputs:
- name: biome_tags_receipt
  type: receipt
- name: biome_tags_artifact
  type: code_fragment
host_requirements:
  capabilities:
  - BIOME
  symbols: []
  schemas: []
implementation_key: fabric/biome/tags
validators:
- java_parse
context_projection:
  max_bytes: 4096
  include:
  - mod_id
  - registry_path
  - package_path
  - biome_properties
repair:
  scope:
  - minecraft/biome/tags
  writable_anchors:
  - /* MMM:biome_tags */
consumes:
- artifact_requirement
- target_cell
- verified_bindings
- owned_anchor
- prerequisite_receipts
provides:
- tags
anchor_kinds:
- symbol
- test
rules:
- Implement tags responsibility for biome strictly within declared BIOME capability.
- Target side must remain server; reject cross-side symbol leakage.
- Fail closed if required biome prerequisite receipt is missing; never invent defaults.
- Output must satisfy java_parse validator before completion.
postconditions:
- biome tags matches host version contract
- biome tags receipts emitted with verified context
