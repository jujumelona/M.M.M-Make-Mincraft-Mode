id: minecraft/menu/slots
task: Implement only the slots responsibility of the supplied menu artifact.
execution_mode: model
side:
- common
inputs:
- name: menu_identity
  type: identifier
  required: true
outputs:
- name: menu_slots_receipt
  type: receipt
host_requirements:
  capabilities:
  - SCREEN_HANDLER
implementation_key: fabric/menu/slots
validators:
- java_parse
context_projection:
  max_bytes: 4096
  include:
  - mod_id
  - registry_path
  - package_path
repair:
  scope:
  - minecraft/menu/slots
  writable_anchors:
  - /* MMM:menu_slots */
consumes:
- artifact_requirement
- target_cell
- verified_bindings
- owned_anchor
- prerequisite_receipts
provides:
- slots
anchor_kinds:
- symbol
- test
rules:
- Implement slots responsibility for menu strictly within declared SCREEN_HANDLER
  capability.
- Target side must remain common; reject cross-side symbol leakage.
- Fail closed if required menu prerequisite receipt is missing; never invent defaults.
- Output must satisfy java_parse validator before completion.
