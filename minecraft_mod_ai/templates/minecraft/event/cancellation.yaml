id: minecraft/event/cancellation
task: Implement only the cancellation responsibility of the supplied event artifact.
execution_mode: model
side:
- common
inputs:
- name: event_identity
  type: identifier
  required: true
outputs:
- name: event_cancellation_receipt
  type: receipt
host_requirements:
  capabilities:
  - EVENT_LISTENER
implementation_key: fabric/event/cancellation
validators:
- java_parse
context_projection:
  max_bytes: 4096
  include:
  - mod_id
  - registry_path
  - package_path
repair:
  scope:
  - minecraft/event/cancellation
  writable_anchors:
  - /* MMM:event_cancellation */
consumes:
- artifact_requirement
- target_cell
- verified_bindings
- owned_anchor
- prerequisite_receipts
provides:
- cancellation
anchor_kinds:
- symbol
- test
rules:
- Implement cancellation responsibility for event strictly within declared EVENT_LISTENER
  capability.
- Target side must remain common; reject cross-side symbol leakage.
- Fail closed if required event prerequisite receipt is missing; never invent defaults.
- Output must satisfy java_parse validator before completion.
