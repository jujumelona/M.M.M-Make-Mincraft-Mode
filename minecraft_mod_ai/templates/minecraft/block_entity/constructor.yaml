id: minecraft/block_entity/constructor
task: Implement only the constructor responsibility of the supplied block entity artifact.
execution_mode: model
side:
- common
inputs:
- name: block_entity_identity
  type: identifier
  required: true
outputs:
- name: block_entity_constructor_receipt
  type: receipt
host_requirements:
  capabilities:
  - BLOCK_ENTITY
implementation_key: fabric/block_entity/constructor
validators:
- java_parse
context_projection:
  max_bytes: 4096
  include:
  - mod_id
  - registry_path
  - package_path
repair:
  scope:
  - minecraft/block_entity/constructor
  writable_anchors:
  - /* MMM:block_entity_constructor */
consumes:
- artifact_requirement
- target_cell
- verified_bindings
- owned_anchor
- prerequisite_receipts
provides:
- constructor
anchor_kinds:
- symbol
- test
rules:
- Implement constructor responsibility for block_entity strictly within declared BLOCK_ENTITY
  capability.
- Target side must remain common; reject cross-side symbol leakage.
- Fail closed if required block_entity prerequisite receipt is missing; never invent
  defaults.
- Output must satisfy java_parse validator before completion.
