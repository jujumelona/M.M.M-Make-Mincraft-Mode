id: minecraft/animation/integration
task: Implement only the integration responsibility of the supplied animation artifact.
execution_mode: model
side:
- client
inputs:
- name: animation_identity
  type: identifier
  required: true
- name: animation_integration_input
  type: specification
  required: true
outputs:
- name: animation_integration_receipt
  type: receipt
- name: animation_integration_artifact
  type: code_fragment
host_requirements:
  capabilities:
  - ENTITY_EXISTS
  symbols:
  - EntityType
  schemas: []
implementation_key: fabric/animation/integration
validators:
- java_parse
- client_side_only
context_projection:
  max_bytes: 4096
  include:
  - mod_id
  - registry_path
  - package_path
  - animation_properties
repair:
  scope:
  - minecraft/animation/integration
  writable_anchors:
  - /* MMM:animation_integration */
consumes:
- artifact_requirement
- target_cell
- verified_bindings
- owned_anchor
- prerequisite_receipts
provides:
- integration
anchor_kinds:
- resource
- test
rules:
- Implement integration responsibility for animation strictly within declared ANIMATION_DEFINITION
  capability.
- Target side must remain client; reject cross-side symbol leakage.
- Fail closed if required animation prerequisite receipt is missing; never invent
  defaults.
- Output must satisfy java_parse validator before completion.
postconditions:
- animation integration matches host version contract
- animation integration receipts emitted with verified context
