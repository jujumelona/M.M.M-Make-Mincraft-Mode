id: minecraft/tag/datagen
task: Implement only the datagen responsibility of the supplied tag artifact.
execution_mode: deterministic
side:
- common
inputs:
- name: tag_identity
  type: identifier
  required: true
- name: tag_datagen_input
  type: specification
  required: true
outputs:
- name: tag_datagen_receipt
  type: receipt
- name: tag_datagen_artifact
  type: code_fragment
host_requirements:
  capabilities:
  - TAGS
  symbols: []
  schemas:
  - fabric/tag/registry
implementation_key: fabric/tag/datagen
validators:
- json_schema
context_projection:
  max_bytes: 4096
  include:
  - mod_id
  - registry_path
  - package_path
  - tag_properties
repair:
  scope:
  - minecraft/tag/datagen
  writable_anchors:
  - /* MMM:tag_datagen */
consumes:
- artifact_requirement
- target_cell
- verified_bindings
- owned_anchor
- prerequisite_receipts
provides:
- datagen
anchor_kinds:
- resource
- test
rules:
- Implement datagen responsibility for tag strictly within declared TAGS capability.
- Target side must remain common; reject cross-side symbol leakage.
- Fail closed if required tag prerequisite receipt is missing; never invent defaults.
- Output must satisfy json_schema validator before completion.
postconditions:
- tag datagen matches host version contract
- tag datagen receipts emitted with verified context
