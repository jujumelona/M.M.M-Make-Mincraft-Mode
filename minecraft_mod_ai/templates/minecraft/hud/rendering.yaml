id: minecraft/hud/rendering
task: Implement only the rendering responsibility of the supplied hud artifact.
execution_mode: model
side:
- client
inputs:
- name: hud_identity
  type: identifier
  required: true
outputs:
- name: hud_rendering_receipt
  type: receipt
host_requirements:
  capabilities:
  - HUD_RENDER
implementation_key: fabric/hud/rendering
validators:
- java_parse
- client_side_only
context_projection:
  max_bytes: 4096
  include:
  - mod_id
  - registry_path
  - package_path
repair:
  scope:
  - minecraft/hud/rendering
  writable_anchors:
  - /* MMM:hud_rendering */
consumes:
- artifact_requirement
- target_cell
- verified_bindings
- owned_anchor
- prerequisite_receipts
provides:
- rendering
anchor_kinds:
- symbol
- test
rules:
- Implement rendering responsibility for hud strictly within declared HUD_RENDER capability.
- Target side must remain client; reject cross-side symbol leakage.
- Fail closed if required hud prerequisite receipt is missing; never invent defaults.
- Output must satisfy java_parse validator before completion.
