id: minecraft/mob/integration
task: Implement only the integration responsibility of the supplied mob artifact.
execution_mode: model
side:
- common
inputs:
- name: mob_identity
  type: identifier
  required: true
outputs:
- name: mob_integration_receipt
  type: receipt
host_requirements:
  capabilities:
  - REGISTER_MOB
  symbols:
  - entity_attributes_register
implementation_key: fabric/mob/integration
validators:
- java_parse
context_projection:
  max_bytes: 4096
  include:
  - mod_id
  - registry_path
  - package_path
repair:
  scope:
  - minecraft/mob/integration
  writable_anchors:
  - /* MMM:mob_integration */
consumes:
- artifact_requirement
- target_cell
- verified_bindings
- owned_anchor
- prerequisite_receipts
provides:
- integration
anchor_kinds:
- symbol
- test
rules:
- Implement integration responsibility for mob strictly within declared REGISTER_MOB
  capability.
- Target side must remain common; reject cross-side symbol leakage.
- Fail closed if required mob prerequisite receipt is missing; never invent defaults.
- Output must satisfy java_parse validator before completion.
