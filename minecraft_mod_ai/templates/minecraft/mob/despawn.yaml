id: minecraft/mob/despawn
task: Implement only the despawn responsibility of the supplied mob artifact.
execution_mode: model
side:
- common
inputs:
- name: mob_identity
  type: identifier
  required: true
outputs:
- name: mob_despawn_receipt
  type: receipt
host_requirements:
  capabilities:
  - REGISTER_MOB
implementation_key: fabric/mob/despawn
validators:
- java_parse
context_projection:
  max_bytes: 4096
  include:
  - mod_id
  - registry_path
  - package_path
repair:
  scope:
  - minecraft/mob/despawn
  writable_anchors:
  - /* MMM:mob_despawn */
consumes:
- artifact_requirement
- target_cell
- verified_bindings
- owned_anchor
- prerequisite_receipts
provides:
- despawn
anchor_kinds:
- symbol
- test
rules:
- Implement despawn responsibility for mob strictly within declared REGISTER_MOB capability.
- Target side must remain common; reject cross-side symbol leakage.
- Fail closed if required mob prerequisite receipt is missing; never invent defaults.
- Output must satisfy java_parse validator before completion.
