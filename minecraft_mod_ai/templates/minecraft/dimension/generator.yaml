id: minecraft/dimension/generator
task: Implement only the generator responsibility of the supplied dimension artifact.
execution_mode: model
side:
- server
inputs:
- name: dimension_identity
  type: identifier
  required: true
outputs:
- name: dimension_generator_receipt
  type: receipt
host_requirements:
  capabilities:
  - DIMENSION
implementation_key: fabric/dimension/generator
validators:
- java_parse
context_projection:
  max_bytes: 4096
  include:
  - mod_id
  - registry_path
  - package_path
repair:
  scope:
  - minecraft/dimension/generator
  writable_anchors:
  - /* MMM:dimension_generator */
consumes:
- artifact_requirement
- target_cell
- verified_bindings
- owned_anchor
- prerequisite_receipts
provides:
- generator
anchor_kinds:
- symbol
- test
rules:
- Implement generator responsibility for dimension strictly within declared DIMENSION
  capability.
- Target side must remain server; reject cross-side symbol leakage.
- Fail closed if required dimension prerequisite receipt is missing; never invent
  defaults.
- Output must satisfy java_parse validator before completion.
