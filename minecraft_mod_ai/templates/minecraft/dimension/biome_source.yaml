id: minecraft/dimension/biome_source
task: Implement only the biome source responsibility of the supplied dimension artifact.
execution_mode: model
side:
- common
inputs:
- name: dimension_identity
  type: identifier
  required: true
- name: dimension_biome_source_input
  type: specification
  required: true
outputs:
- name: dimension_biome_source_receipt
  type: receipt
- name: dimension_biome_source_artifact
  type: code_fragment
host_requirements:
  capabilities:
  - DIMENSION
  symbols: []
  schemas: []
implementation_key: fabric/dimension/biome_source
validators:
- java_parse
context_projection:
  max_bytes: 4096
  include:
  - mod_id
  - registry_path
  - package_path
  - dimension_properties
repair:
  scope:
  - minecraft/dimension/biome_source
  writable_anchors:
  - /* MMM:dimension_biome_source */
consumes:
- artifact_requirement
- target_cell
- verified_bindings
- owned_anchor
- prerequisite_receipts
provides:
- biome_source
anchor_kinds:
- symbol
- test
rules:
- Implement biome_source responsibility for dimension strictly within declared DIMENSION
  capability.
- Target side must remain server; reject cross-side symbol leakage.
- Fail closed if required dimension prerequisite receipt is missing; never invent
  defaults.
- Output must satisfy java_parse validator before completion.
postconditions:
- dimension biome_source matches host version contract
- dimension biome_source receipts emitted with verified context
