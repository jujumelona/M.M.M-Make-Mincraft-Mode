id: code/generation_unit
input:
  file_target:
  class_contract:
  method_contract:
  field_contract:
  registration_operation:
  call_edges:
  dependencies:
  imports:
task:
  assemble_one_generation_unit:
rules:
  - Generate only the owned anchor for this responsibility.
  - Block when a required contract remains unresolved.
output:
  generation_unit:
  blocked_reasons:
proof:
  predicate: the unit is bounded to one owned anchor and all required inputs are resolved
