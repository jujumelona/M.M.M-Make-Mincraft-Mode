id: code/dependency_plan
input:
  code_unit:
  required_capability:
  verified_bindings:
task:
  select_one_dependency:
rules:
  - Use an existing project or verified target dependency when sufficient.
  - Never invent coordinates or symbols.
output:
  dependency:
  unresolved_dependency:
proof:
  predicate: every selected dependency has a verified provider
