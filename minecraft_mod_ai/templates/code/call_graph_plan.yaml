id: code/call_graph_plan
input:
  producer:
  consumer:
  behavior_contract:
task:
  define_one_call_edge:
rules:
  - Add only a call edge required by the supplied behavior.
  - Preserve dependency direction and logical-side boundary.
output:
  call_edge:
proof:
  predicate: the edge has one verified producer, consumer, and reason
