id: code/registration_plan
input:
  artifact:
  registry_requirement:
  target_bindings:
task:
  define_registration_operation:
rules:
  - Use only verified target registry bindings.
  - Keep identifier ownership explicit and collision-checked.
output:
  registration_operation:
  blocked_reason:
proof:
  predicate: registration has a verified registry surface, owned identifier, and initialization point
