id: integration/call_graph_connect
input:
  call_edges:
  generation_units:
task:
  assemble_call_graph:
rules:
  - Preserve declared ordering, ownership, and dependency direction.
  - Reject missing callers, callees, and unintended recursion.
output:
  call_graph:
  invalid_edges:
proof:
  predicate: every call edge resolves to existing owned units and graph constraints pass
