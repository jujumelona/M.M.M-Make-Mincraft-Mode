id: integration/resource_connect
input:
  resource_artifacts:
  code_references:
task:
  connect_resource_references:
rules:
  - Match identifiers and paths exactly.
  - Report missing or orphaned resources rather than fabricating references.
output:
  resource_edges:
  missing_resources:
  orphaned_resources:
proof:
  predicate: every required code-resource reference resolves exactly once
