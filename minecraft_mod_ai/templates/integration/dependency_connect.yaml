id: integration/dependency_connect
input:
  dependency_plan:
  generation_units:
task:
  connect_dependency_edges:
rules:
  - Connect only declared provider-consumer dependencies.
  - Reject cycles unless an explicit runtime indirection contract resolves them.
output:
  dependency_edges:
  cycles:
proof:
  predicate: every edge has one provider and consumer and the resulting graph is valid
