id: integration/client_server_connect
input:
  side_mapping:
  network_mapping:
task:
  connect_logical_side_boundaries:
rules:
  - Preserve server authority and client presentation boundaries.
  - Cross-side mutation requires an explicit validated network path.
output:
  side_edges:
  authority_violations:
proof:
  predicate: every cross-side edge has a valid authority and synchronization contract
