id: integration/registration
input:
  registration_operations:
task:
  connect_registration_operations:
rules:
  - Preserve initialization order and identifier ownership.
  - Do not register the same identifier twice.
output:
  registration_edges:
  conflicts:
proof:
  predicate: every registration has one owner and conflict-free initialization path
