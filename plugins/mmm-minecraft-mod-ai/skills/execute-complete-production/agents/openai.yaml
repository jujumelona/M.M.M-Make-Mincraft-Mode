interface:
  display_name: "Execute Complete Production"
  short_description: "Run a complete mod through resumable evidence gates."
  default_prompt: "Use $execute-complete-production to build the approved mod and close every required quality gate."
