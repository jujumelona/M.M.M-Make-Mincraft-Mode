id: design/content_scale
slot_id: content_scale
task: "Define the content footprint in terms of items, blocks, and entities."
rules:
  - "Must cite concrete Minecraft interaction mechanics."
  - "Must stay strictly under 256 characters."
record_schema:
  type: object
  properties:
    content_scale:
      type: string
      maxLength: 256
  required:
    - content_scale
  additionalProperties: false
