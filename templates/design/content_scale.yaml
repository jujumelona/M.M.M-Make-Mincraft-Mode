id: design/content_scale
slot_id: content_scale
task: "Define the content footprint in terms of items, blocks, and entities."
rules:
  - "Use only explicit requirements and supplied context; do not invent content."
record_schema:
  type: object
  properties:
    content_scale:
      type: string
      maxLength: 256
  required:
    - content_scale
  additionalProperties: false
