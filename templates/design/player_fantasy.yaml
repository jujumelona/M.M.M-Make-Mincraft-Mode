id: design/player_fantasy
slot_id: player_fantasy
task: "Define the core role and fantasy experienced by the player."
rules:
  - "Use only explicit requirements and supplied context; do not invent roles, themes, or mechanics."
record_schema:
  type: object
  properties:
    player_fantasy:
      type: string
      maxLength: 256
  required:
    - player_fantasy
  additionalProperties: false
