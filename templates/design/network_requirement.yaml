id: design/network_requirement
slot_id: network_requirement
task: "Define client-server synchronized packets, HUD states, or effects."
rules:
  - "Must cite concrete Minecraft interaction mechanics."
  - "Must stay strictly under 256 characters."
record_schema:
  type: object
  properties:
    network_requirement:
      type: string
      maxLength: 256
  required:
    - network_requirement
  additionalProperties: false
