id: design/npc_role
slot_id: npc_role
task: "Define required non-player character interactions, dialogue, or companion behavior."
rules:
  - "Use only explicit requirements and supplied context; do not invent NPCs, dialogue, relationships, or companion mechanics."
record_schema:
  type: object
  properties:
    npc_role:
      type: string
      maxLength: 256
  required:
    - npc_role
  additionalProperties: false
