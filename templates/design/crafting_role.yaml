id: design/crafting_role
slot_id: crafting_role
task: "Define recipe stations, crafting steps, or assembly mechanics."
rules:
  - "Must cite concrete Minecraft interaction mechanics."
  - "Must stay strictly under 256 characters."
record_schema:
  type: object
  properties:
    crafting_role:
      type: string
      maxLength: 256
  required:
    - crafting_role
  additionalProperties: false
