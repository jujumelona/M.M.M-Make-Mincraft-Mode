id: design/world_interaction
slot_id: world_interaction
task: "Define unique physics, environmental hazards, or world interactions."
rules:
  - "Must cite concrete Minecraft interaction mechanics."
  - "Must stay strictly under 256 characters."
record_schema:
  type: object
  properties:
    world_interaction:
      type: string
      maxLength: 256
  required:
    - world_interaction
  additionalProperties: false
