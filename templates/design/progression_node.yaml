id: design/progression_node
slot_id: progression_node
task: "Define the primary milestone node in the progression tree."
rules:
  - "Must cite concrete Minecraft interaction mechanics."
  - "Must stay strictly under 256 characters."
record_schema:
  type: object
  properties:
    progression_node:
      type: string
      maxLength: 256
  required:
    - progression_node
  additionalProperties: false
