id: design/enemy_role
slot_id: enemy_role
task: "Define hostile entity behaviors or boss threats, or none if peaceful."
rules:
  - "Must cite concrete Minecraft interaction mechanics."
  - "Must stay strictly under 256 characters."
record_schema:
  type: object
  properties:
    enemy_role:
      type: string
      maxLength: 256
  required:
    - enemy_role
  additionalProperties: false
