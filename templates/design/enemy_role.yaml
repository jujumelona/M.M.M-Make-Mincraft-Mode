id: design/enemy_role
slot_id: enemy_role
task: "Define hostile entity behaviors or boss threats when combat opposition is explicitly required."
rules:
  - "Use only explicit requirements and supplied context; do not invent enemies, bosses, or combat mechanics."
record_schema:
  type: object
  properties:
    enemy_role:
      type: string
      maxLength: 256
  required:
    - enemy_role
  additionalProperties: false
