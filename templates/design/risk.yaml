id: design/risk
slot_id: risk
task: "Define the threat, risk, or resource depletion faced by the player."
rules:
  - "Must cite concrete Minecraft interaction mechanics."
  - "Must stay strictly under 256 characters."
record_schema:
  type: object
  properties:
    risk:
      type: string
      maxLength: 256
  required:
    - risk
  additionalProperties: false
