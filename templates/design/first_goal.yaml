id: design/first_goal
slot_id: first_goal
task: "Define the initial immediate player objective upon starting the mod."
rules:
  - "Use only explicit requirements and supplied context; do not invent objectives or prerequisites."
record_schema:
  type: object
  properties:
    first_goal:
      type: string
      maxLength: 256
  required:
    - first_goal
  additionalProperties: false
