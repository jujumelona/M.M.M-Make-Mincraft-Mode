id: design/visual_identity
slot_id: visual_identity
task: "Define the explicitly required art style, palette, and particle aesthetic for this mod."
rules:
  - "Use only explicit requirements and supplied context; do not invent palette, art direction, particles, or mechanics."
record_schema:
  type: object
  properties:
    visual_identity:
      type: string
      maxLength: 256
  required:
    - visual_identity
  additionalProperties: false
