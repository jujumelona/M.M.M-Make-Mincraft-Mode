id: design/visual_identity
slot_id: visual_identity
task: "Define art style, palette, and particle aesthetic for this mod."
rules:
  - "Must cite concrete Minecraft interaction mechanics."
  - "Must stay strictly under 256 characters."
record_schema:
  type: object
  properties:
    visual_identity:
      type: string
      maxLength: 256
  required:
    - visual_identity
  additionalProperties: false
