id: design/ui_requirement
slot_id: ui_requirement
task: "Define custom screens, container menus, or HUD overlays explicitly required by the requested behavior."
rules:
  - "Use only explicit requirements and supplied context; do not invent screens, menus, HUD elements, or interactions."
record_schema:
  type: object
  properties:
    ui_requirement:
      type: string
      maxLength: 256
  required:
    - ui_requirement
  additionalProperties: false
