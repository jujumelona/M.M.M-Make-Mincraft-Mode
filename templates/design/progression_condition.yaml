id: design/progression_condition
slot_id: progression_condition
task: "Define the explicit condition required to progress to the next gameplay tier."
rules:
  - "Use only explicit requirements and supplied context; do not invent gates, tiers, resources, or objectives."
record_schema:
  type: object
  properties:
    progression_condition:
      type: string
      maxLength: 256
  required:
    - progression_condition
  additionalProperties: false
