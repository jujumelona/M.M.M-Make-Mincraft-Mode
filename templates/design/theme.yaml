id: design/theme
slot_id: theme
task: "Define the primary thematic domain and setting of this mod."
rules:
  - "Must cite concrete Minecraft interaction mechanics."
  - "Must stay strictly under 256 characters."
record_schema:
  type: object
  properties:
    theme:
      type: string
      maxLength: 256
  required:
    - theme
  additionalProperties: false
