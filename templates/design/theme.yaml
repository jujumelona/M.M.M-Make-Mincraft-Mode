id: design/theme
slot_id: theme
task: "Define the primary thematic domain and setting of this mod."
rules:
  - "Use only explicit requirements and supplied context; do not invent setting details, lore, or mechanics."
record_schema:
  type: object
  properties:
    theme:
      type: string
      maxLength: 256
  required:
    - theme
  additionalProperties: false
