id: design/texture_requirement
slot_id: texture_requirement
task: "Define the primary item and block textures needed for this mod."
rules:
  - "Must cite concrete Minecraft interaction mechanics."
  - "Must stay strictly under 256 characters."
record_schema:
  type: object
  properties:
    texture_requirement:
      type: string
      maxLength: 256
  required:
    - texture_requirement
  additionalProperties: false
