id: design/core_loop_step
slot_id: core_loop_step
task: "Define the sequential steps forming the cyclical gameplay loop."
rules:
  - "Must cite concrete Minecraft interaction mechanics."
  - "Must stay strictly under 256 characters."
record_schema:
  type: object
  properties:
    core_loop_step:
      type: string
      maxLength: 256
  required:
    - core_loop_step
  additionalProperties: false
