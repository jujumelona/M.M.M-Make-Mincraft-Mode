input:
  artifact_kinds: []

input_schema:
  artifact_kinds: list[string]

task:
  expand_required_minecraft_dependencies: true

rules:
  - Dependencies are defined only between canonical Minecraft artifact kinds.
  - Do not add dependencies from gameplay names or semantic categories.
  - Preserve deterministic dependency-before-dependent order.
  - Emit each artifact kind exactly once.

output:
  artifact_kinds: []

output_schema:
  artifact_kinds: list[string]

proof:
  predicate: output is canonical, duplicate-free, and dependency-closed
