id: asset/item_sprite
task: Supply item framing to PromptCompiler using resolved contracts and visual semantics.
requires:
  - resolved_resource_contract
  - resolved_generation_profile
  - visual_spec
rules:
  - Consume HOST geometry and model binding without inferring replacements.
  - PromptCompiler supplies registry settings and semantic VisualSpec.
  - This fragment owns framing only; it must not own model IDs, LoRA triggers or dimensions.
render:
  language: text
  body: "isolated item sprite, single object, centered, transparent background, no environment, no inventory slot"
