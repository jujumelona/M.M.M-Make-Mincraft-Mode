id: design/goal_prerequisite
slot_id: goal_prerequisite
task: "Define the prerequisite condition or materials required before the first goal."
rules:
  - "Use only explicit requirements and supplied context; do not invent prerequisites."
record_schema:
  type: object
  properties:
    goal_prerequisite:
      type: string
      minLength: 1
      maxLength: 256
  required:
    - goal_prerequisite
  additionalProperties: false
