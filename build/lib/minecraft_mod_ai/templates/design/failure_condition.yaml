id: design/failure_condition
slot_id: failure_condition
task: "Define the loss state or hazard condition the player must avoid or manage."
rules:
  - "Use only explicit requirements and supplied context; do not invent hazards."
record_schema:
  type: object
  properties:
    failure_condition:
      type: string
      minLength: 1
      maxLength: 256
  required:
    - failure_condition
  additionalProperties: false
