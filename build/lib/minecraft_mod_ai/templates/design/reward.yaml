id: design/reward
slot_id: reward
task: "Define the explicit reward or unlocked capability granted by the progression goal."
rules:
  - "Use only explicit requirements and supplied context; do not invent rewards, unlocks, items, or capabilities."
record_schema:
  type: object
  properties:
    reward:
      type: string
      minLength: 1
      maxLength: 256
  required:
    - reward
  additionalProperties: false
