id: design/audio_identity
slot_id: audio_identity
task: "Define ambient soundscapes, sound effects, or musical cues."
rules:
  - "Use only explicit requirements and supplied context; do not invent mechanics."
record_schema:
  type: object
  properties:
    audio_identity:
      type: string
      minLength: 1
      maxLength: 256
  required:
    - audio_identity
  additionalProperties: false
