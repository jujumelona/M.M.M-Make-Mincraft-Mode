id: design/progression_edge
slot_id: progression_edge
task: "Define the explicit trigger or requirement linking progression nodes."
rules:
  - "Use only explicit requirements and supplied context; do not invent nodes, prerequisites, rewards, or gates."
record_schema:
  type: object
  properties:
    progression_edge:
      type: string
      minLength: 1
      maxLength: 256
  required:
    - progression_edge
  additionalProperties: false
