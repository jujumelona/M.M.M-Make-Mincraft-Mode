id: design/risk
slot_id: risk
task: "Define the explicit threat, risk, or resource depletion faced by the player."
rules:
  - "Use only explicit requirements and supplied context; do not invent hazards, enemies, depletion systems, or penalties."
record_schema:
  type: object
  properties:
    risk:
      type: string
      minLength: 1
      maxLength: 256
  required:
    - risk
  additionalProperties: false
