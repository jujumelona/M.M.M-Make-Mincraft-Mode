id: design/resource_source
slot_id: resource_source
task: "Define where and how explicitly required foundational resources are gathered."
rules:
  - "Use only explicit requirements and supplied context; do not invent resources, locations, drops, or gathering mechanics."
record_schema:
  type: object
  properties:
    resource_source:
      type: string
      minLength: 1
      maxLength: 256
  required:
    - resource_source
  additionalProperties: false
