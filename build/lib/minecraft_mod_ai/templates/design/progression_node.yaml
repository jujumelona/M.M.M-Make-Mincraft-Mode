id: design/progression_node
slot_id: progression_node
task: "Define one primary milestone node in the progression tree."
rules:
  - "Use only explicit requirements and supplied context; do not invent milestones, rewards, prerequisites, or mechanics."
record_schema:
  type: object
  properties:
    progression_node:
      type: string
      minLength: 1
      maxLength: 256
  required:
    - progression_node
  additionalProperties: false
