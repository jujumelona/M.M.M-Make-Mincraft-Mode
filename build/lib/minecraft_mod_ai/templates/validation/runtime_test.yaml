id: validation/runtime_test
execution: host
input:
  runtime_scenarios:
  target_runtime:
output:
  passed:
  observations:
  failures:
proof:
  predicate: passed is true only when required observable runtime outcomes are recorded
