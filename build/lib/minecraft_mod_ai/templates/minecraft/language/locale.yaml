id: minecraft/language/locale
task: Implement only the locale responsibility of the supplied language artifact.
execution_mode: model
side:
- common
inputs:
- name: language_identity
  type: identifier
  required: true
- name: language_locale_input
  type: specification
  required: true
outputs:
- name: language_locale_receipt
  type: receipt
- name: language_locale_artifact
  type: code_fragment
host_requirements:
  capabilities:
  - REGISTER_ITEM
  symbols: []
  schemas:
  - fabric/item/lang_en
implementation_key: fabric/language/locale
validators:
- java_parse
context_projection:
  max_bytes: 4096
  include:
  - mod_id
  - registry_path
  - package_path
  - language_properties
repair:
  scope:
  - minecraft/language/locale
  writable_anchors:
  - /* MMM:language_locale */
consumes:
- artifact_requirement
- target_cell
- verified_bindings
- owned_anchor
- prerequisite_receipts
provides:
- locale
anchor_kinds:
- resource
- test
rules:
- Implement locale responsibility for language strictly within declared LANGUAGE_ASSET
  capability.
- Target side must remain common; reject cross-side symbol leakage.
- Fail closed if required language prerequisite receipt is missing; never invent defaults.
- Output must satisfy json_schema validator before completion.
postconditions:
- language locale matches host version contract
- language locale receipts emitted with verified context
