id: minecraft/advancement/parent
task: Implement only the parent responsibility of the supplied advancement artifact.
execution_mode: model
side:
- common
inputs:
- name: advancement_identity
  type: identifier
  required: true
- name: advancement_parent_input
  type: specification
  required: true
outputs:
- name: advancement_parent_receipt
  type: receipt
- name: advancement_parent_artifact
  type: code_fragment
host_requirements:
  capabilities:
  - ADVANCEMENT
  symbols: []
  schemas: []
implementation_key: fabric/advancement/parent
validators:
- java_parse
context_projection:
  max_bytes: 4096
  include:
  - mod_id
  - registry_path
  - package_path
  - advancement_properties
repair:
  scope:
  - minecraft/advancement/parent
  writable_anchors:
  - /* MMM:advancement_parent */
consumes:
- artifact_requirement
- target_cell
- verified_bindings
- owned_anchor
- prerequisite_receipts
provides:
- parent
anchor_kinds:
- resource
- test
rules:
- Implement parent responsibility for advancement strictly within declared ADVANCEMENT
  capability.
- Target side must remain common; reject cross-side symbol leakage.
- Fail closed if required advancement prerequisite receipt is missing; never invent
  defaults.
- Output must satisfy java_parse validator before completion.
postconditions:
- advancement parent matches host version contract
- advancement parent receipts emitted with verified context
