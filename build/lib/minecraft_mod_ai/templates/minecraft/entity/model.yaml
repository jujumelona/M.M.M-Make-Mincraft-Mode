id: minecraft/entity/model
task: Implement only the model responsibility of the supplied entity artifact.
execution_mode: model
side:
- client
inputs:
- name: entity_identity
  type: identifier
  required: true
- name: entity_model_input
  type: specification
  required: true
outputs:
- name: entity_model_receipt
  type: receipt
- name: entity_model_artifact
  type: code_fragment
host_requirements:
  capabilities:
  - ENTITY_EXISTS
  symbols:
  - EntityType
  schemas: []
implementation_key: fabric/entity/model
validators:
- java_parse
- client_side_only
context_projection:
  max_bytes: 4096
  include:
  - mod_id
  - registry_path
  - package_path
  - entity_properties
repair:
  scope:
  - minecraft/entity/model
  writable_anchors:
  - /* MMM:entity_model */
consumes:
- artifact_requirement
- target_cell
- verified_bindings
- owned_anchor
- prerequisite_receipts
provides:
- model
anchor_kinds:
- symbol
- test
rules:
- Implement model responsibility for entity strictly within declared REGISTER_ENTITY
  capability.
- Target side must remain client; reject cross-side symbol leakage.
- Fail closed if required entity prerequisite receipt is missing; never invent defaults.
- Output must satisfy java_parse validator before completion.
postconditions:
- entity model matches host version contract
- entity model receipts emitted with verified context
