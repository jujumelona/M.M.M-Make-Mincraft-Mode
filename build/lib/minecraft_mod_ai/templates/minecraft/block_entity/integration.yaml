id: minecraft/block_entity/integration
task: Implement only the integration responsibility of the supplied block entity artifact.
execution_mode: model
side:
- common
inputs:
- name: block_entity_identity
  type: identifier
  required: true
- name: block_entity_integration_input
  type: specification
  required: true
outputs:
- name: block_entity_integration_receipt
  type: receipt
- name: block_entity_integration_artifact
  type: code_fragment
host_requirements:
  capabilities:
  - BLOCK_ENTITY_EXISTS
  - REGISTER_BLOCK
  symbols:
  - BlockEntityType
  - register_block
  schemas: []
implementation_key: fabric/block_entity/integration
validators:
- java_parse
context_projection:
  max_bytes: 4096
  include:
  - mod_id
  - registry_path
  - package_path
  - block_entity_properties
repair:
  scope:
  - minecraft/block_entity/integration
  writable_anchors:
  - /* MMM:block_entity_integration */
consumes:
- artifact_requirement
- target_cell
- verified_bindings
- owned_anchor
- prerequisite_receipts
provides:
- integration
anchor_kinds:
- symbol
- test
rules:
- Implement integration responsibility for block_entity strictly within declared BLOCK_ENTITY
  capability.
- Target side must remain common; reject cross-side symbol leakage.
- Fail closed if required block_entity prerequisite receipt is missing; never invent
  defaults.
- Output must satisfy java_parse validator before completion.
postconditions:
- block_entity integration matches host version contract
- block_entity integration receipts emitted with verified context
