id: minecraft/item/language
task: Implement only the language responsibility of the supplied item artifact.
execution_mode: model
side:
- common
inputs:
- name: item_identity
  type: identifier
  required: true
- name: item_language_input
  type: specification
  required: true
outputs:
- name: item_language_receipt
  type: receipt
- name: item_language_artifact
  type: code_fragment
host_requirements:
  capabilities:
  - REGISTER_ITEM
  - ITEM_SETTINGS
  symbols:
  - register_item
  - resource_key_create
  - item_stacks_to
  schemas: []
implementation_key: fabric/item/language
validators:
- java_parse
context_projection:
  max_bytes: 4096
  include:
  - mod_id
  - registry_path
  - package_path
  - item_properties
repair:
  scope:
  - minecraft/item/language
  writable_anchors:
  - /* MMM:item_language */
consumes:
- artifact_requirement
- target_cell
- verified_bindings
- owned_anchor
- prerequisite_receipts
provides:
- language
anchor_kinds:
- symbol
- test
rules:
- Implement language responsibility for item strictly within declared REGISTER_ITEM
  capability.
- Target side must remain common; reject cross-side symbol leakage.
- Fail closed if required item prerequisite receipt is missing; never invent defaults.
- Output must satisfy java_parse validator before completion.
postconditions:
- item language matches host version contract
- item language receipts emitted with verified context
