id: minecraft/inventory/transaction
task: Implement only the transaction responsibility of the supplied inventory artifact.
execution_mode: model
side:
- common
inputs:
- name: inventory_identity
  type: identifier
  required: true
- name: inventory_transaction_input
  type: specification
  required: true
outputs:
- name: inventory_transaction_receipt
  type: receipt
- name: inventory_transaction_artifact
  type: code_fragment
host_requirements:
  capabilities:
  - REGISTER_ITEM
  symbols:
  - register_item
  schemas: []
implementation_key: fabric/inventory/transaction
validators:
- java_parse
context_projection:
  max_bytes: 4096
  include:
  - mod_id
  - registry_path
  - package_path
  - inventory_properties
repair:
  scope:
  - minecraft/inventory/transaction
  writable_anchors:
  - /* MMM:inventory_transaction */
consumes:
- artifact_requirement
- target_cell
- verified_bindings
- owned_anchor
- prerequisite_receipts
provides:
- transaction
anchor_kinds:
- symbol
- test
rules:
- Implement transaction responsibility for inventory strictly within declared INVENTORY_CAPABILITY
  capability.
- Target side must remain common; reject cross-side symbol leakage.
- Fail closed if required inventory prerequisite receipt is missing; never invent
  defaults.
- Output must satisfy java_parse validator before completion.
postconditions:
- inventory transaction matches host version contract
- inventory transaction receipts emitted with verified context
