id: minecraft/inventory/slots
task: Implement only the slots responsibility of the supplied inventory artifact.
execution_mode: model
side:
- common
inputs:
- name: inventory_identity
  type: identifier
  required: true
- name: inventory_slots_input
  type: specification
  required: true
outputs:
- name: inventory_slots_receipt
  type: receipt
- name: inventory_slots_artifact
  type: code_fragment
host_requirements:
  capabilities:
  - REGISTER_ITEM
  symbols:
  - register_item
  schemas: []
implementation_key: fabric/inventory/slots
validators:
- java_parse
context_projection:
  max_bytes: 4096
  include:
  - mod_id
  - registry_path
  - package_path
  - inventory_properties
repair:
  scope:
  - minecraft/inventory/slots
  writable_anchors:
  - /* MMM:inventory_slots */
consumes:
- artifact_requirement
- target_cell
- verified_bindings
- owned_anchor
- prerequisite_receipts
provides:
- slots
anchor_kinds:
- symbol
- test
rules:
- Implement slots responsibility for inventory strictly within declared INVENTORY_CAPABILITY
  capability.
- Target side must remain common; reject cross-side symbol leakage.
- Fail closed if required inventory prerequisite receipt is missing; never invent
  defaults.
- Output must satisfy java_parse validator before completion.
postconditions:
- inventory slots matches host version contract
- inventory slots receipts emitted with verified context
