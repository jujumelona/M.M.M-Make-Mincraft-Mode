id: minecraft/tag/identifier
task: Implement only the identifier responsibility of the supplied tag artifact.
execution_mode: model
side:
- common
inputs:
- name: tag_identity
  type: identifier
  required: true
- name: tag_identifier_input
  type: specification
  required: true
outputs:
- name: tag_identifier_receipt
  type: receipt
- name: tag_identifier_artifact
  type: code_fragment
host_requirements:
  capabilities:
  - TAGS
  symbols: []
  schemas:
  - fabric/tag/registry
implementation_key: fabric/tag/identifier
validators:
- java_parse
context_projection:
  max_bytes: 4096
  include:
  - mod_id
  - registry_path
  - package_path
  - tag_properties
repair:
  scope:
  - minecraft/tag/identifier
  writable_anchors:
  - /* MMM:tag_identifier */
consumes:
- artifact_requirement
- target_cell
- verified_bindings
- owned_anchor
- prerequisite_receipts
provides:
- identifier
anchor_kinds:
- resource
- test
rules:
- Implement identifier responsibility for tag strictly within declared TAGS capability.
- Target side must remain common; reject cross-side symbol leakage.
- Fail closed if required tag prerequisite receipt is missing; never invent defaults.
- Output must satisfy java_parse validator before completion.
postconditions:
- tag identifier matches host version contract
- tag identifier receipts emitted with verified context
