from __future__ import annotations

from pathlib import Path


def replace_once(path: str, old: str, new: str) -> None:
    target = Path(path)
    text = target.read_text(encoding="utf-8")
    count = text.count(old)
    if count != 1:
        raise SystemExit(f"{path}: expected exactly one match, found {count}: {old[:120]!r}")
    target.write_text(text.replace(old, new, 1), encoding="utf-8")


proof = "minecraft_mod_ai/proof_level.py"
replace_once(proof, '''    if dst == ProofLevel.SUBGRAPH_COMPILE_VERIFIED and not _positive_int(data, "verified_subgraphs"):\n        return False, "INVALID_RECEIPT: SUBGRAPH_COMPILE_VERIFIED requires verified_subgraphs>0"\n''', '''    if dst == ProofLevel.SUBGRAPH_COMPILE_VERIFIED:\n        if not _positive_int(data, "verified_subgraphs"):\n            return False, "INVALID_RECEIPT: SUBGRAPH_COMPILE_VERIFIED requires verified_subgraphs>0"\n        if data.get("authoritative_compile") is not True:\n            return False, "INVALID_RECEIPT: SUBGRAPH_COMPILE_VERIFIED requires authoritative compile execution"\n''')
replace_once(proof, '''    if dst == ProofLevel.COMPILE_VERIFIED and data.get("compile_passed") is not True:\n        return False, "INVALID_RECEIPT: COMPILE_VERIFIED requires compile_passed=true"\n''', '''    if dst == ProofLevel.COMPILE_VERIFIED:\n        if data.get("compile_passed") is not True:\n            return False, "INVALID_RECEIPT: COMPILE_VERIFIED requires compile_passed=true"\n        if data.get("authoritative_compile") is not True:\n            return False, "INVALID_RECEIPT: COMPILE_VERIFIED requires authoritative compile execution"\n''')

executor = "minecraft_mod_ai/reuse_proof_executor.py"
replace_once(executor, '''    verified_capabilities: tuple[str, ...]\n    residual_capabilities: tuple[str, ...]\n    dependency_receipts: tuple[Mapping[str, Any], ...] = ()\n''', '''    verified_capabilities: tuple[str, ...]\n    residual_capabilities: tuple[str, ...]\n    authoritative_compile: bool = False\n    failure_scope: str = ""\n    failure_code: str = ""\n    failure_message: str = ""\n    dependency_receipts: tuple[Mapping[str, Any], ...] = ()\n''')
replace_once(executor, '''            "verified_capabilities": list(self.verified_capabilities),\n            "residual_capabilities": list(self.residual_capabilities),\n            "dependency_receipts": [dict(item) for item in self.dependency_receipts],\n''', '''            "verified_capabilities": list(self.verified_capabilities),\n            "residual_capabilities": list(self.residual_capabilities),\n            "authoritative_compile": self.authoritative_compile,\n            "failure_scope": self.failure_scope,\n            "failure_code": self.failure_code,\n            "failure_message": self.failure_message,\n            "dependency_receipts": [dict(item) for item in self.dependency_receipts],\n''')
replace_once(executor, '''def _license_rejected_receipt(\n    donor_slice: DonorSlice,\n    *,\n    candidate_id: str,\n    closure_hash: str,\n) -> ReuseProofReceipt:\n    return ReuseProofReceipt(\n        candidate_id=candidate_id,\n        capability=donor_slice.capability,\n        commit_sha=donor_slice.commit_sha,\n        closure_hash=closure_hash,\n        proof_level=ProofLevel.DISCOVERED.value,\n        compile_passed=False,\n        tests_passed=False,\n        unresolved_symbols=(),\n        missing_resources=(),\n        adaptations_applied=(),\n        verified_capabilities=(),\n        residual_capabilities=(donor_slice.capability,),\n    )\n''', '''def _rejected_receipt(\n    donor_slice: DonorSlice,\n    *,\n    candidate_id: str,\n    closure_hash: str,\n    failure_code: str,\n    failure_message: str = "",\n    failure_scope: str = "donor",\n    proof_level: ProofLevel = ProofLevel.DISCOVERED,\n    missing_resources: Sequence[str] = (),\n) -> ReuseProofReceipt:\n    return ReuseProofReceipt(\n        candidate_id=candidate_id,\n        capability=donor_slice.capability,\n        commit_sha=donor_slice.commit_sha,\n        closure_hash=closure_hash,\n        proof_level=proof_level.value,\n        compile_passed=False,\n        tests_passed=False,\n        unresolved_symbols=(),\n        missing_resources=tuple(missing_resources),\n        adaptations_applied=(),\n        verified_capabilities=(),\n        residual_capabilities=(donor_slice.capability,),\n        failure_scope=failure_scope,\n        failure_code=failure_code,\n        failure_message=failure_message,\n    )\n''')
replace_once(executor, '''    try:\n        validate_donor_slice_manifest(donor_slice)\n    except SourceTransplantError:\n        return _license_rejected_receipt(\n            donor_slice, candidate_id=candidate_id, closure_hash=closure_hash\n        )\n\n    if not is_reusable_source_license(donor_slice.license_id):\n        return _license_rejected_receipt(\n            donor_slice,\n            candidate_id=candidate_id,\n            closure_hash=closure_hash,\n        )\n''', '''    try:\n        validate_donor_slice_manifest(donor_slice)\n    except SourceTransplantError as exc:\n        return _rejected_receipt(\n            donor_slice, candidate_id=candidate_id, closure_hash=closure_hash,\n            failure_code="MANIFEST_INVALID", failure_message=str(exc),\n        )\n\n    if not is_reusable_source_license(donor_slice.license_id):\n        return _rejected_receipt(\n            donor_slice, candidate_id=candidate_id, closure_hash=closure_hash,\n            failure_code="LICENSE_NOT_REUSABLE",\n            failure_message=f"License {donor_slice.license_id!r} is not approved for source reuse.",\n        )\n''')
replace_once(executor, '''    if not valid:\n        return _license_rejected_receipt(\n            donor_slice,\n            candidate_id=candidate_id,\n            closure_hash=closure_hash,\n        )\n    current_level = ProofLevel.LICENSE_VERIFIED\n''', '''    if not valid:\n        return _rejected_receipt(\n            donor_slice, candidate_id=candidate_id, closure_hash=closure_hash,\n            failure_code="LICENSE_PROOF_INVALID",\n        )\n    current_level = ProofLevel.LICENSE_VERIFIED\n''')
replace_once(executor, '''    if not valid:\n        return ReuseProofReceipt(\n            candidate_id=candidate_id, capability=donor_slice.capability,\n            commit_sha=donor_slice.commit_sha, closure_hash=closure_hash,\n            proof_level=current_level.value, compile_passed=False, tests_passed=False,\n            unresolved_symbols=(), missing_resources=(), adaptations_applied=(),\n            verified_capabilities=(), residual_capabilities=(donor_slice.capability,),\n        )\n    current_level = ProofLevel.PINNED\n''', '''    if not valid:\n        return _rejected_receipt(\n            donor_slice, candidate_id=candidate_id, closure_hash=closure_hash,\n            proof_level=current_level, failure_code="PIN_INVALID",\n        )\n    current_level = ProofLevel.PINNED\n''')
replace_once(executor, '''    in_memory_files: dict[str, str | bytes] = {}\n    materialization_failed = False\n    try:\n''', '''    in_memory_files: dict[str, str | bytes] = {}\n    materialization_failed = False\n    materialization_error = ""\n    try:\n''')
replace_once(executor, '''    except SourceTransplantError:\n        if not in_memory_files:\n            materialization_failed = True\n\n    if materialization_failed:\n        return ReuseProofReceipt(\n            candidate_id=candidate_id,\n            capability=donor_slice.capability,\n            commit_sha=donor_slice.commit_sha,\n            closure_hash=closure_hash,\n            proof_level=current_level.value,\n            compile_passed=False,\n            tests_passed=False,\n            unresolved_symbols=(),\n            missing_resources=tuple(\n                edge.requested_target for edge in donor_slice.unresolved_edges\n            ),\n            adaptations_applied=(),\n            verified_capabilities=(),\n            residual_capabilities=(donor_slice.capability,),\n        )\n''', '''    except SourceTransplantError as exc:\n        if not in_memory_files:\n            materialization_failed = True\n            materialization_error = str(exc)\n\n    if materialization_failed:\n        return _rejected_receipt(\n            donor_slice, candidate_id=candidate_id, closure_hash=closure_hash,\n            proof_level=current_level, failure_code="MATERIALIZATION_FAILED",\n            failure_message=materialization_error,\n            missing_resources=tuple(\n                edge.requested_target for edge in donor_slice.unresolved_edges\n            ),\n        )\n''')
replace_once(executor, '''    dependency_injection_failed = False\n\n    with tempfile.TemporaryDirectory() as sandbox_dir:\n''', '''    dependency_injection_failed = False\n    authoritative_compile_execution = not callable(compile_checker)\n\n    with tempfile.TemporaryDirectory() as sandbox_dir:\n''')
replace_once(executor, '''    authoritative_host_execution = not callable(compile_checker)\n''', '''    authoritative_host_execution = authoritative_compile_execution\n''')
replace_once(executor, '''    if (\n        compile_passed\n        and donor_slice.closure_complete\n        and not unresolved_mandatory_deps\n    ):\n''', '''    if (\n        authoritative_compile_execution\n        and compile_passed\n        and donor_slice.closure_complete\n        and not unresolved_mandatory_deps\n    ):\n''')
replace_once(executor, '''            comp_files = {path: adapted_files[path] for path in component}\n            comp_passed = False\n            if callable(compile_checker):\n                comp_result = compile_checker(comp_files, target_context)\n                if isinstance(comp_result, Mapping):\n                    comp_passed = bool(comp_result.get("compile_passed"))\n                else:\n                    comp_passed = bool(comp_result)\n            else:\n                with tempfile.TemporaryDirectory(prefix="mmm_subgraph_") as sub_tmp:\n''', '''            comp_files = {path: adapted_files[path] for path in component}\n            comp_passed = False\n            if callable(compile_checker):\n                # Caller-supplied checkers are diagnostic-only and cannot mint\n                # reusable subgraph proof. Avoid repeating the diagnostic per subgraph.\n                residual_art_list.extend(component)\n                continue\n            else:\n                with tempfile.TemporaryDirectory(prefix="mmm_subgraph_") as sub_tmp:\n''')
replace_once(executor, '''    if (\n        compile_passed\n        and donor_slice.closure_complete\n        and not unresolved_mandatory_deps\n        and not dependency_injection_failed\n    ):\n        valid, _ = validate_proof_transition(\n            current_level,\n            ProofLevel.COMPILE_VERIFIED,\n            receipt={"compile_passed": True},\n        )\n''', '''    if (\n        authoritative_compile_execution\n        and compile_passed\n        and donor_slice.closure_complete\n        and not unresolved_mandatory_deps\n        and not dependency_injection_failed\n    ):\n        valid, _ = validate_proof_transition(\n            current_level,\n            ProofLevel.COMPILE_VERIFIED,\n            receipt={"compile_passed": True, "authoritative_compile": True},\n        )\n''')
replace_once(executor, '''        subgraph_valid, _ = validate_proof_transition(\n            current_level,\n            ProofLevel.SUBGRAPH_COMPILE_VERIFIED,\n            receipt={"verified_subgraphs": verified_subgraph_count},\n        )\n''', '''        subgraph_valid, _ = validate_proof_transition(\n            current_level, ProofLevel.SUBGRAPH_COMPILE_VERIFIED,\n            receipt={"verified_subgraphs": verified_subgraph_count, "authoritative_compile": True},\n        )\n''')
replace_once(executor, '''    return ReuseProofReceipt(\n        candidate_id=candidate_id,\n        capability=donor_slice.capability,\n        commit_sha=donor_slice.commit_sha,\n        closure_hash=closure_hash,\n        proof_level=current_level.value,\n''', '''    failure_scope = ""\n    failure_code = ""\n    failure_message = ""\n    if not current_level.allows_reuse():\n        if not authoritative_compile_execution:\n            failure_scope = "verification"\n            failure_code = "NON_AUTHORITATIVE_COMPILE_CHECKER"\n            failure_message = "Caller-supplied compile_checker is diagnostic-only."\n        elif dependency_injection_failed:\n            failure_scope = "dependency"\n            failure_code = "BUILD_MODEL_RENDER_FAILED"\n        elif unresolved_mandatory_deps:\n            failure_scope = "dependency"\n            failure_code = "DEPENDENCY_UNRESOLVED"\n        elif not adapted_files:\n            failure_scope = "donor"\n            failure_code = "NO_MATERIALIZED_ARTIFACTS"\n        elif not compile_passed:\n            failure_scope = "verification"\n            failure_code = "COMPILE_FAILED"\n\n    return ReuseProofReceipt(\n        candidate_id=candidate_id,\n        capability=donor_slice.capability,\n        commit_sha=donor_slice.commit_sha,\n        closure_hash=closure_hash,\n        proof_level=current_level.value,\n''')
replace_once(executor, '''        compile_passed=(\n            compile_passed\n            and donor_slice.closure_complete\n''', '''        compile_passed=(\n            authoritative_compile_execution\n            and compile_passed\n            and donor_slice.closure_complete\n''')
replace_once(executor, '''        verified_capabilities=verified_caps,\n        residual_capabilities=residual_caps,\n        dependency_receipts=tuple(\n''', '''        verified_capabilities=verified_caps,\n        residual_capabilities=residual_caps,\n        authoritative_compile=authoritative_compile_execution,\n        failure_scope=failure_scope,\n        failure_code=failure_code,\n        failure_message=failure_message,\n        dependency_receipts=tuple(\n''')
replace_once(executor, '''    receipts: list[ReuseProofReceipt] = []\n    best_partial: tuple[tuple[int, int, int, int, int], DonorSlice] | None = None\n\n    for candidate in candidates:\n        receipt = execute_reuse_proof(\n''', '''    requested_capability = str(capability or "").strip()\n    if not requested_capability:\n        raise ValueError("capability must be non-empty")\n\n    receipts: list[ReuseProofReceipt] = []\n    best_partial: tuple[tuple[int, int, int, int, int], DonorSlice] | None = None\n\n    for candidate in candidates:\n        if str(candidate.capability or "").strip() != requested_capability:\n            receipts.append(_rejected_receipt(\n                candidate, candidate_id=f"{candidate.repository}@{candidate.commit_sha}",\n                closure_hash=_closure_sha256(candidate), failure_code="CAPABILITY_MISMATCH",\n                failure_message=(f"Candidate capability {candidate.capability!r} does not match "\n                                 f"requested capability {requested_capability!r}."),\n            ))\n            continue\n\n        receipt = execute_reuse_proof(\n''')

artifacts = "minecraft_mod_ai/reuse_artifacts.py"
replace_once(artifacts, '''    if bundle.origin_kind == "external_donor":\n        if str(_receipt_value(receipt, "candidate_id", "")).strip() != bundle.source_ref:\n            return False\n''', '''    if bundle.origin_kind == "external_donor":\n        if _receipt_value(receipt, "authoritative_compile") is not True:\n            return False\n        if str(_receipt_value(receipt, "candidate_id", "")).strip() != bundle.source_ref:\n            return False\n''')

test_path = Path("tests/test_worker6_reuse_authority.py")
if test_path.exists():
    raise SystemExit(f"{test_path}: already exists")
test_path.write_text('''from __future__ import annotations\n\nimport hashlib\n\nimport pytest\n\nimport minecraft_mod_ai.reuse_proof_executor as reuse_proof\nimport minecraft_mod_ai.source_transplant as source_transplant\nfrom minecraft_mod_ai.proof_level import ProofLevel, validate_proof_transition\nfrom minecraft_mod_ai.reuse_artifacts import ReusableArtifactBundle, bundle_proof_allows_reuse\nfrom minecraft_mod_ai.source_transplant import DonorFile, DonorSlice\n\n\ndef _donor(capability: str = "boss.entity") -> DonorSlice:\n    payload = b"package donor; public class BossEntity {}\\n"\n    return DonorSlice(capability=capability, repository="example/authority", commit_sha="a" * 40, license_id="MIT", source_url="https://github.com/example/authority", target_compatibility="metadata_exact", files=(DonorFile(path="src/main/java/donor/BossEntity.java", blob_sha="b" * 40, sha256="sha256:" + hashlib.sha256(payload).hexdigest(), size_bytes=len(payload), symbols=("BossEntity",)),), seed_files=("src/main/java/donor/BossEntity.java",), source_symbols=("BossEntity",), required_dependencies=(), donor_tests=(), confidence=0.9, closure_complete=True)\n\n\ndef test_compile_transition_requires_authoritative_executor():\n    valid, reason = validate_proof_transition(ProofLevel.MATERIALIZED, ProofLevel.COMPILE_VERIFIED, receipt={"compile_passed": True})\n    assert valid is False and "authoritative" in reason\n    valid, _ = validate_proof_transition(ProofLevel.MATERIALIZED, ProofLevel.COMPILE_VERIFIED, receipt={"compile_passed": True, "authoritative_compile": True})\n    assert valid is True\n\n\ndef test_subgraph_transition_requires_authoritative_executor():\n    valid, reason = validate_proof_transition(ProofLevel.MATERIALIZED, ProofLevel.SUBGRAPH_COMPILE_VERIFIED, receipt={"verified_subgraphs": 1})\n    assert valid is False and "authoritative" in reason\n    valid, _ = validate_proof_transition(ProofLevel.MATERIALIZED, ProofLevel.SUBGRAPH_COMPILE_VERIFIED, receipt={"verified_subgraphs": 1, "authoritative_compile": True})\n    assert valid is True\n\n\ndef test_compile_checker_is_diagnostic_only(monkeypatch, tmp_path):\n    payload = b"package donor; public class BossEntity {}\\n"\n    monkeypatch.setattr(source_transplant, "materialize_pinned_donor", lambda *args, **kwargs: {"src/main/java/donor/BossEntity.java": payload})\n    receipt = reuse_proof.execute_reuse_proof(_donor(), target_workspace=tmp_path, target_context={}, compile_checker=lambda *_: {"compile_passed": True, "tests_passed": True})\n    assert receipt.authoritative_compile is False\n    assert receipt.compile_passed is False\n    assert not ProofLevel.from_value(receipt.proof_level).allows_reuse()\n    assert receipt.failure_scope == "verification"\n    assert receipt.failure_code == "NON_AUTHORITATIVE_COMPILE_CHECKER"\n\n\ndef test_fallback_rejects_capability_mismatch_without_proof_execution(monkeypatch, tmp_path):\n    donor = _donor("other.capability")\n    monkeypatch.setattr(reuse_proof, "execute_reuse_proof", lambda *args, **kwargs: (_ for _ in ()).throw(AssertionError("must not execute")))\n    selected, receipts = reuse_proof.execute_candidate_fallback_loop((donor,), "boss.entity", target_workspace=tmp_path, target_context={})\n    assert selected is None\n    assert len(receipts) == 1\n    assert receipts[0].failure_scope == "donor"\n    assert receipts[0].failure_code == "CAPABILITY_MISMATCH"\n\n\ndef test_fallback_requires_nonempty_capability(tmp_path):\n    with pytest.raises(ValueError, match="capability must be non-empty"):\n        reuse_proof.execute_candidate_fallback_loop((), "  ", target_workspace=tmp_path, target_context={})\n\n\ndef test_external_bundle_rejects_non_authoritative_compile_receipt():\n    donor = _donor()\n    path, digest = donor.files[0].path, donor.files[0].sha256\n    source_ref = f"{donor.repository}@{donor.commit_sha}"\n    bundle = ReusableArtifactBundle(bundle_id="donor:test", capability=donor.capability, origin_kind="external_donor", source_ref=source_ref, file_hashes={path: digest}, protected_paths=(path,), provenance={"repository": donor.repository, "commit_sha": donor.commit_sha, "license_id": donor.license_id, "donor_slice": donor.to_dict()})\n    base_receipt = {"proof_level": ProofLevel.COMPILE_VERIFIED.value, "capability": donor.capability, "candidate_id": source_ref, "compile_passed": True, "dependency_receipts": (), "contract": {"protected_artifacts": {path: digest}}}\n    assert bundle_proof_allows_reuse(bundle, base_receipt) is False\n    assert bundle_proof_allows_reuse(bundle, dict(base_receipt, authoritative_compile=True)) is True\n\n\ndef test_invalid_manifest_has_structured_donor_failure(monkeypatch, tmp_path):\n    donor = _donor()\n    invalid = DonorSlice(**{**donor.__dict__, "commit_sha": "not-a-pin"})\n    monkeypatch.setattr(source_transplant, "materialize_pinned_donor", lambda *args, **kwargs: (_ for _ in ()).throw(AssertionError("must not materialize")))\n    receipt = reuse_proof.execute_reuse_proof(invalid, target_workspace=tmp_path, target_context={}, compile_checker=lambda *_: True)\n    assert receipt.failure_scope == "donor"\n    assert receipt.failure_code == "MANIFEST_INVALID"\n    assert receipt.compile_passed is False\n''', encoding="utf-8")
