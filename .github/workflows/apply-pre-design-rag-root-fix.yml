name: Apply pre-design RAG root fix

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/apply-pre-design-rag-root-fix.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: pip
      - run: python -m pip install --prefer-binary -e '.[dev]'
      - name: Replace split-brain pre-design retrieval with one grounded owner
        run: |
          python - <<'PY'
          from pathlib import Path

          path = Path('minecraft_mod_ai/pre_design_research_pipeline.py')
          text = path.read_text(encoding='utf-8')
          text = text.replace('import json\nimport traceback\n', 'import hashlib\nimport json\nimport re\nimport traceback\n', 1)
          text = text.replace('from concurrent.futures import Future, ThreadPoolExecutor\n', '', 1)
          text = text.replace(
              'from .central_research import normalize_research_brief, retrieve_domain_evidence\n',
              'from .central_research import normalize_research_brief\n',
              1,
          )
          text = text.replace('from .pre_design_local_project_evidence import collect_local_project_evidence\n', '', 1)
          old_stages = '''_DETERMINISTIC_STAGES = (\n    "official_rag",\n    "technology_radar",\n    "forced_project_rag",\n)\n'''
          new_stages = '''_CONTENT_FIELDS = ("content", "excerpt", "snippet", "text", "body")\n_QUERY_STOP_TERMS = frozenset(\n    {\n        "fabric", "minecraft", "mod", "mods", "requested", "existing", "host",\n        "resolved", "target", "implementation", "mechanic", "system", "game",\n        "player", "players", "official", "documentation", "docs", "source",\n    }\n)\n'''
          if old_stages not in text:
              raise SystemExit('deterministic-stage anchor drifted')
          text = text.replace(old_stages, new_stages, 1)

          start = text.index('def _target_neutral_official_evidence(')
          end = text.index('def _validate_document_grounding(', start)
          replacement = r'''def _evidence_tokens(value: Any) -> set[str]:
    folded = re.sub(r"[_./:+-]+", " ", str(value).casefold())
    return {
        token
        for token in re.findall(r"[a-z0-9]+|[가-힣]{2,}", folded)
        if len(token) > 1 and token not in _QUERY_STOP_TERMS
    }


def _document_relevance(query: str, document: Mapping[str, Any]) -> int:
    query_tokens = _evidence_tokens(query)
    if not query_tokens:
        return 0
    searchable = " ".join(
        str(document.get(field, ""))
        for field in ("document_id", "source_id", "title", "url", "topics", "content")
    )
    return len(query_tokens & _evidence_tokens(searchable))


def _target_neutral_official_evidence(
    research_brief: Mapping[str, Any],
) -> dict[str, Any]:
    """Return only query-relevant reviewed documents, never the whole corpus/TOC."""

    index = OfficialCorpusIndex(BUILTIN_CORPUS)
    reviewed = [
        {**document.public_metadata(), "content": document.content}
        for document in index.documents
    ]
    domains: list[dict[str, Any]] = []
    raw_domains = research_brief.get("domains", [])
    if isinstance(raw_domains, list):
        for domain in raw_domains:
            if not isinstance(domain, Mapping):
                continue
            raw_queries = domain.get("queries", [])
            queries = [str(item).strip() for item in raw_queries if str(item).strip()] if isinstance(raw_queries, list) else []
            query_text = " ".join([str(domain.get("objective", "")), *queries])
            scored = [
                (_document_relevance(query_text, document), document)
                for document in reviewed
            ]
            selected = [
                document
                for score, document in sorted(
                    scored,
                    key=lambda item: (-item[0], str(item[1].get("document_id", ""))),
                )
                if score > 0
            ][:4]
            domains.append(
                {
                    "domain_id": str(domain.get("domain_id", "")),
                    "queries": queries,
                    "documents": selected,
                }
            )
    return {
        "schema_version": "mmm/pre-design-official-evidence",
        "status": "available",
        "target_scope": "target_neutral",
        "target_frozen": False,
        "corpus_snapshot_hash": index.snapshot_hash,
        "document_count": sum(len(item["documents"]) for item in domains),
        "domains": domains,
    }


def _builtin_content_record(record: Mapping[str, Any], query: str) -> dict[str, Any] | None:
    source_id = str(record.get("source_id") or record.get("document_id") or "").strip()
    for document in BUILTIN_CORPUS:
        document_id = str(getattr(document, "document_id", "")).strip()
        if not source_id or source_id != document_id:
            continue
        candidate = {**document.public_metadata(), "source_id": source_id, "content": document.content}
        if _document_relevance(query, candidate) <= 0:
            return None
        candidate["evidence_origin"] = "official_reviewed_document"
        return candidate
    return None


def _record_content(record: Mapping[str, Any]) -> str:
    for field in _CONTENT_FIELDS:
        value = record.get(field)
        if isinstance(value, str) and len(value.strip()) >= 24:
            return value.strip()
    return ""


def _section_records(section: Any) -> list[Mapping[str, Any]]:
    if not isinstance(section, Mapping):
        return []
    rows: list[Mapping[str, Any]] = []
    for key in ("documents", "hits", "sources", "records", "results"):
        value = section.get(key)
        if isinstance(value, list):
            rows.extend(item for item in value if isinstance(item, Mapping))
    return rows


def _query_content_records(query_item: Mapping[str, Any]) -> list[dict[str, Any]]:
    query = str(query_item.get("query", ""))
    records: list[dict[str, Any]] = []
    seen: set[str] = set()
    for section_name in ("external_rag", "code_rag", "project_rag"):
        section = query_item.get(section_name)
        for raw in _section_records(section):
            item = dict(raw)
            content = _record_content(item)
            if not content and section_name == "project_rag":
                materialized = _builtin_content_record(item, query)
                if materialized is not None:
                    item = materialized
                    content = _record_content(item)
            if not content:
                continue
            digest = str(item.get("content_sha256", "")).strip()
            if not digest:
                digest = "sha256:" + hashlib.sha256(content.encode("utf-8")).hexdigest()
                item["content_sha256"] = digest
            if digest in seen:
                continue
            seen.add(digest)
            item["retrieval_section"] = section_name
            records.append(item)
    return records


def _is_github_record(record: Mapping[str, Any]) -> bool:
    source_id = str(record.get("source_id", "")).casefold()
    url = str(record.get("url", "")).casefold()
    metadata = record.get("metadata")
    repository = str(metadata.get("repository", "")).strip() if isinstance(metadata, Mapping) else ""
    return source_id.startswith("github:") or "github.com/" in url or bool(repository)


def _grounded_domain_evidence(agentic: Any, domain_id: str, bundle: Mapping[str, Any]) -> dict[str, Any]:
    selected = agentic._domain_source_value(domain_id, bundle)
    queries: list[dict[str, Any]] = []
    raw_queries = selected.get("queries", []) if isinstance(selected, Mapping) else []
    if isinstance(raw_queries, list):
        for item in raw_queries:
            if not isinstance(item, Mapping):
                continue
            records = _query_content_records(item)
            queries.append(
                {
                    "query": str(item.get("query", "")),
                    "query_sha256": str(item.get("query_sha256", "")),
                    "evidence_records": records,
                    "content_record_count": len(records),
                    "github_record_count": sum(1 for record in records if _is_github_record(record)),
                }
            )
    return {
        "schema_version": "mmm/pre-design-grounded-domain-evidence",
        "domain_id": domain_id,
        "queries": queries,
    }


def _grounded_rag_receipt(bundle: Mapping[str, Any]) -> dict[str, Any]:
    domains: list[dict[str, Any]] = []
    for domain in bundle.get("domains", []) if isinstance(bundle.get("domains"), list) else []:
        if not isinstance(domain, Mapping):
            continue
        queries: list[dict[str, Any]] = []
        for item in domain.get("queries", []) if isinstance(domain.get("queries"), list) else []:
            if not isinstance(item, Mapping):
                continue
            records = _query_content_records(item)
            queries.append(
                {
                    "query": str(item.get("query", "")),
                    "query_sha256": str(item.get("query_sha256", "")),
                    "content_record_count": len(records),
                    "github_record_count": sum(1 for record in records if _is_github_record(record)),
                }
            )
        domains.append({"domain_id": str(domain.get("domain_id", "")), "queries": queries})
    return {
        "schema_version": "mmm/pre-design-grounded-rag-receipt",
        "status": "available",
        "research_sha256": bundle.get("research_sha256"),
        "domain_count": len(domains),
        "domains": domains,
        "source_content_omitted": True,
    }


def _validate_domain_provider_grounding(
    domain: Mapping[str, Any],
    grounded: Mapping[str, Any],
) -> None:
    records = [
        record
        for query in grounded.get("queries", [])
        if isinstance(query, Mapping)
        for record in query.get("evidence_records", [])
        if isinstance(record, Mapping)
    ]
    domain_id = str(domain.get("domain_id", "")).strip() or "unknown"
    if not records:
        raise PreDesignResearchFailure(
            f"Pre-design retrieval gap for domain {domain_id!r}: retrieval returned only "
            "catalog/TOC/metadata or empty hits; no claim-bearing source content was retrieved."
        )
    providers = {
        str(item).casefold()
        for item in domain.get("providers", [])
        if str(item).strip()
    }
    if "github" in providers and not any(_is_github_record(record) for record in records):
        raise PreDesignResearchFailure(
            f"Pre-design retrieval gap for domain {domain_id!r}: required provider 'github' "
            "returned no content-bearing source document."
        )


def _domain_document_evidence(
    agentic: Any,
    domain_id: str,
    deterministic: Mapping[str, Any],
    *,
    grounded_bundle: Mapping[str, Any],
) -> dict[str, Any]:
    result: dict[str, Any] = {
        "grounded_rag": _grounded_domain_evidence(agentic, domain_id, grounded_bundle)
    }
    for source, value in deterministic.items():
        if source == "grounded_rag":
            continue
        if isinstance(value, Mapping) and str(value.get("status", "")) in {
            "deferred_until_target_freeze",
            "unavailable",
        }:
            continue
        result[str(source)] = agentic._domain_source_value(domain_id, value)
    return result


'''
          text = text[:start] + replacement + text[end:]

          block_start = text.index('    target_frozen = _target_frozen(knowledge_plan)\n', text.index('def collect_design_research('))
          block_end = text.index('    domain_notes: list[dict[str, Any]] = []\n', block_start)
          new_block = r'''    target_frozen = _target_frozen(knowledge_plan)
    try:
        # One retrieval owner for the live planner path. Runtime contracts wrap this
        # owner with bounded external GitHub/Modrinth source retrieval and local RAG.
        grounded_bundle = project_rag._forced_rag_bundle(router, research_brief)
        deterministic["grounded_rag"] = _grounded_rag_receipt(grounded_bundle)
        _emit_research_diagnostic(
            "deterministic_stage_complete",
            stage="grounded_rag",
            result=deterministic["grounded_rag"],
        )
    except Exception as exc:
        diagnostic = _exception_payload(exc)
        _emit_research_diagnostic(
            "deterministic_stage_failure",
            stage="grounded_rag",
            exception=diagnostic,
        )
        raise PreDesignResearchFailure(
            "Unified pre-design grounded retrieval failed before evidence extraction: "
            f"{type(exc).__name__}: {exc}"
        ) from exc

    if target_frozen:
        try:
            deterministic["technology_radar"] = collect_technology_radar(
                prompt,
                research_brief,
                page_size=50,
                page_builder=build_technology_radar,
            )
            _emit_research_diagnostic(
                "deterministic_stage_complete",
                stage="technology_radar",
                result=deterministic["technology_radar"],
            )
        except Exception as exc:
            diagnostic = _exception_payload(exc)
            _emit_research_diagnostic(
                "deterministic_stage_failure",
                stage="technology_radar",
                exception=diagnostic,
            )
            errors.append({"stage": "technology_radar", "error": f"{type(exc).__name__}: {exc}"})
            deterministic["technology_radar"] = {"status": "unavailable", "failure": diagnostic}
    else:
        deterministic["technology_radar"] = _deferred_technology_receipt(knowledge_plan)
        _emit_research_diagnostic(
            "deterministic_stage_deferred",
            stage="technology_radar",
            result=deterministic["technology_radar"],
        )

'''
          text = text[:block_start] + new_block + text[block_end:]

          old_call = '''            domain_evidence = _domain_document_evidence(\n                agentic,\n                domain_id,\n                deterministic,\n            )\n'''
          new_call = '''            domain_evidence = _domain_document_evidence(\n                agentic,\n                domain_id,\n                deterministic,\n                grounded_bundle=grounded_bundle,\n            )\n            _validate_domain_provider_grounding(\n                domain,\n                domain_evidence["grounded_rag"],\n            )\n'''
          if old_call not in text:
              raise SystemExit('domain evidence call anchor drifted')
          text = text.replace(old_call, new_call, 1)
          text = text.replace(
              '"adaptive_retrieval": "lossless evidence documents are paged to the model within bounded context",',
              '"adaptive_retrieval": "query-scoped claim-bearing evidence is paged to the model within bounded context",',
              1,
          )
          text = text.replace(
              '"corrective_retrieval": "official and local-project evidence are independently retained in the host ledger",',
              '"corrective_retrieval": "one grounded retrieval owner supplies local, official and external source evidence",',
              1,
          )
          text = text.replace(
              '"planning_search": "third-party donor search is deferred to frozen-design reuse planning",',
              '"planning_search": "pre-design source discovery is grounded; donor selection remains a frozen-design reuse decision",',
              1,
          )
          path.write_text(text, encoding='utf-8')

          knowledge = Path('minecraft_mod_ai/minecraft_knowledge_nodes.py')
          text = knowledge.read_text(encoding='utf-8')
          old = '''    deterministic = research.get('deterministic')\n    forced = deterministic.get('forced_project_rag') if isinstance(deterministic, Mapping) else None\n    forced_map = {str(x.get('domain_id', '')): x for x in (forced.get('domains', []) if isinstance(forced, Mapping) else []) if isinstance(x, Mapping)}\n    official = deterministic.get('official_rag') if isinstance(deterministic, Mapping) else None\n'''
          new = '''    deterministic = research.get('deterministic')\n    grounded = deterministic.get('grounded_rag') if isinstance(deterministic, Mapping) else None\n    grounded_map = {str(x.get('domain_id', '')): x for x in (grounded.get('domains', []) if isinstance(grounded, Mapping) else []) if isinstance(x, Mapping)}\n    forced = deterministic.get('forced_project_rag') if isinstance(deterministic, Mapping) else None\n    forced_map = {str(x.get('domain_id', '')): x for x in (forced.get('domains', []) if isinstance(forced, Mapping) else []) if isinstance(x, Mapping)}\n    official = deterministic.get('official_rag') if isinstance(deterministic, Mapping) else None\n'''
          if old not in text:
              raise SystemExit('knowledge coverage deterministic anchor drifted')
          text = text.replace(old, new, 1)
          old_candidates = '''        candidates = [\n            ('official_versioned_rag', official_map.get(domain_id)),\n            ('project_rag', forced_map.get(domain_id)),\n        ]\n'''
          new_candidates = '''        candidates = [\n            ('grounded_rag', grounded_map.get(domain_id)),\n            ('official_versioned_rag', official_map.get(domain_id)),\n            ('project_rag', forced_map.get(domain_id)),\n        ]\n'''
          if old_candidates not in text:
              raise SystemExit('knowledge coverage candidate anchor drifted')
          text = text.replace(old_candidates, new_candidates, 1)
          knowledge.write_text(text, encoding='utf-8')

          test = Path('tests/test_pre_design_rag_root_cause.py')
          test.write_text(r'''from __future__ import annotations

from types import SimpleNamespace

import pytest

from minecraft_mod_ai import agentic_pre_design_rag as project_rag
from minecraft_mod_ai import pre_design_research_pipeline as subject


def _domain_value(domain_id, value):
    if not isinstance(value, dict):
        return {}
    for domain in value.get("domains", []):
        if isinstance(domain, dict) and domain.get("domain_id") == domain_id:
            return domain
    return {}


def test_target_neutral_official_rag_is_query_scoped_not_corpus_dump():
    evidence = subject._target_neutral_official_evidence(
        {
            "domains": [
                {
                    "domain_id": "resource",
                    "objective": "gather ore resources and harvesting progression",
                    "queries": ["resource gathering harvesting ore progression"],
                }
            ]
        }
    )
    documents = evidence["domains"][0]["documents"]
    ids = {str(item.get("document_id", "")) for item in documents}
    assert len(documents) < len(subject.BUILTIN_CORPUS)
    assert "fabric-blockbench" not in ids


def test_grounded_domain_evidence_drops_toc_metadata_and_keeps_source_content():
    bundle = {
        "domains": [
            {
                "domain_id": "resource",
                "queries": [
                    {
                        "query": "resource gathering harvesting",
                        "query_sha256": "sha256:q",
                        "project_rag": {
                            "sources": [
                                {
                                    "source_id": "fabric-blockbench",
                                    "title": "Blockbench Documentation",
                                    "url": "https://docs.fabricmc.net/develop/blockbench/",
                                }
                            ]
                        },
                        "external_rag": {
                            "status": "ok",
                            "documents": [
                                {
                                    "source_id": "github:owner/repo:src/Harvest.java",
                                    "url": "https://github.com/owner/repo/blob/main/src/Harvest.java",
                                    "content": "public final class Harvest { void gatherResourceNode() { collectOre(); } }",
                                    "metadata": {"repository": "owner/repo"},
                                }
                            ],
                        },
                    }
                ],
            }
        ]
    }
    agentic = SimpleNamespace(_domain_source_value=_domain_value)
    grounded = subject._grounded_domain_evidence(agentic, "resource", bundle)
    records = grounded["queries"][0]["evidence_records"]
    assert len(records) == 1
    assert records[0]["source_id"].startswith("github:")
    assert "Harvest" in records[0]["content"]
    assert all(record.get("title") != "Blockbench Documentation" for record in records)


def test_required_github_gap_fails_before_model_extraction():
    with pytest.raises(subject.PreDesignResearchFailure, match="github"):
        subject._validate_domain_provider_grounding(
            {"domain_id": "resource", "providers": ["github"]},
            {
                "queries": [
                    {
                        "query": "resource gathering",
                        "evidence_records": [
                            {
                                "source_id": "official:resource-doc",
                                "content": "A sufficiently long official resource gathering explanation for the test.",
                            }
                        ],
                    }
                ]
            },
        )


def test_live_collect_design_research_uses_unified_grounded_owner(monkeypatch):
    calls = []
    brief = {
        "summary": "resource research",
        "domains": [
            {
                "domain_id": "resource",
                "objective": "resource gathering",
                "queries": ["resource gathering"],
                "providers": ["github"],
            }
        ],
        "unresolved_questions": [],
    }
    plan = {
        "policy": {"target_frozen": False},
        "research_domains": [],
        "plan_sha256": "sha256:plan",
    }
    bundle = {
        "schema_version": "mmm/forced-pre-design-rag",
        "research_sha256": "sha256:rag",
        "domains": [
            {
                "domain_id": "resource",
                "queries": [
                    {
                        "query": "resource gathering",
                        "query_sha256": "sha256:q",
                        "external_rag": {
                            "status": "ok",
                            "documents": [
                                {
                                    "source_id": "github:owner/repo:src/Harvest.java",
                                    "url": "https://github.com/owner/repo/blob/main/src/Harvest.java",
                                    "content": "public final class Harvest { void gatherResourceNode() { collectOre(); } }",
                                    "metadata": {"repository": "owner/repo"},
                                }
                            ],
                        },
                    }
                ],
            }
        ],
    }

    monkeypatch.setattr(subject, "_pre_design_brief", lambda prompt: brief)
    monkeypatch.setattr(subject, "compile_minecraft_knowledge_plan", lambda prompt: plan)

    def forced(router, research_brief):
        calls.append((router, research_brief))
        return bundle

    monkeypatch.setattr(project_rag, "_forced_rag_bundle", forced)
    monkeypatch.setattr(
        project_rag,
        "_materialize_domain_evidence_document",
        lambda domain_id, evidence: {"domain_id": domain_id, "evidence": evidence},
    )
    monkeypatch.setattr(subject, "_validate_document_grounding", lambda *args, **kwargs: None)
    monkeypatch.setattr(
        subject,
        "research_document_domain",
        lambda *args, **kwargs: {
            "domain_id": "resource",
            "research_failures": [],
            "sufficient": True,
            "fixed_point": True,
            "checkpoint": {"status": "complete"},
        },
    )
    monkeypatch.setattr(
        subject,
        "evaluate_route_coverage",
        lambda *args, **kwargs: {"status": "PASS", "blocking_requirement_refs": []},
    )
    monkeypatch.setattr(subject, "attach_procedural_skillbank", lambda router, prompt, payload: payload)
    monkeypatch.setattr(subject, "compose_research_skillbank", lambda router, prompt, payload: payload)
    monkeypatch.setattr(
        subject,
        "_bounded_model_view",
        lambda agentic, router, prompt, payload: {"model_view_sha256": "sha256:view", "payload": payload},
    )

    result = subject.collect_design_research(object(), "resource gathering")
    assert len(calls) == 1
    deterministic = result["payload"]["deterministic"]
    assert "grounded_rag" in deterministic
    assert "official_rag" not in deterministic
    assert "forced_project_rag" not in deterministic
''', encoding='utf-8')

          Path('.github/workflows/apply-pre-design-rag-root-fix.yml').unlink(missing_ok=True)
          PY
      - name: Validate root-cause fix
        run: |
          python -m compileall -q minecraft_mod_ai
          python -m ruff check minecraft_mod_ai tests --select F,E7,E9
          python -m pytest -q \
            tests/test_pre_design_rag_root_cause.py \
            tests/test_grounded_rag_runtime_contract.py \
            tests/test_reuse_authoritative_search.py \
            tests/test_reuse_source_read_tool.py \
            tests/test_reuse_research_hardening_contract.py \
            tests/test_semantic_requirement_authority_single_pass.py
      - name: Verify split-brain path is gone
        run: |
          python - <<'PY'
          from pathlib import Path
          text = Path('minecraft_mod_ai/pre_design_research_pipeline.py').read_text(encoding='utf-8')
          body = text[text.index('def collect_design_research('):]
          assert 'collect_local_project_evidence(' not in body
          assert 'retrieve_domain_evidence if target_frozen else _target_neutral_official_evidence' not in body
          assert 'project_rag._forced_rag_bundle(router, research_brief)' in body
          assert 'catalog/TOC/metadata' in text
          PY
      - name: Commit root-cause fix directly to main
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: unify pre-design grounded retrieval ownership"
          git push origin HEAD:main
