name: Update liveness tests once

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/update_liveness_tests_once.yml

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: pip
      - run: python -m pip install --prefer-binary -e '.[dev]'
      - run: python .github/scripts/update_liveness_tests_once.py
      - run: |
          python -m py_compile \
            tests/test_custom_module_output_continuation.py \
            tests/test_custom_module_structural_repair_efficiency.py \
            tests/test_unified_host_execution_loop.py
      - run: |
          python -m pytest -q \
            tests/test_custom_module_output_continuation.py \
            tests/test_custom_module_structural_repair_efficiency.py \
            tests/test_unified_host_execution_loop.py \
            tests/test_coder_liveness_boundaries.py
      - name: Commit tests and remove temporary files
        run: |
          git rm .github/scripts/update_liveness_tests_once.py .github/workflows/update_liveness_tests_once.yml
          git add tests/test_custom_module_output_continuation.py tests/test_custom_module_structural_repair_efficiency.py tests/test_unified_host_execution_loop.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "test: align coder liveness contracts with single-owner recovery"
          git push origin HEAD:main
      - name: Remove temporary files after failure
        if: failure()
        run: |
          git reset --hard HEAD
          git rm -f .github/scripts/update_liveness_tests_once.py .github/workflows/update_liveness_tests_once.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "chore: remove failed liveness test machinery"
          git push origin HEAD:main
