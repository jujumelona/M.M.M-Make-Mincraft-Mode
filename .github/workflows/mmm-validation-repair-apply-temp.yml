name: MMM build ledger apply

on:
  push:
    paths:
      - '.github/workflows/mmm-validation-repair-apply-temp.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Apply build ledger ordering patch
        run: python .github/scripts/mmm_fix_build_ledger_order_temp.py
      - name: Compile
        run: python -m compileall -q minecraft_mod_ai tests/test_validation_repair_regressions.py
      - name: Install test dependencies
        run: python -m pip install -e '.[dev]'
      - name: Run focused regressions
        run: python -m pytest -q tests/test_validation_repair_regressions.py tests/test_project_manifest_hash_efficiency_contract.py
      - name: Commit production fix and clean temporary files
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          rm -f .github/workflows/mmm-validation-repair-apply-temp.yml
          rm -f .github/scripts/mmm_fix_build_ledger_order_temp.py
          git add minecraft_mod_ai/complete_orchestrator.py tests/test_validation_repair_regressions.py .github/workflows .github/scripts
          git diff --cached --check
          git commit -m 'fix: order build ledger before final validation'
          for attempt in 1 2 3; do
            git fetch origin main
            if ! git rebase origin/main; then
              git rebase --abort || true
              echo 'Concurrent main change conflicted with tested patch; refusing force push.' >&2
              exit 1
            fi
            if git push origin HEAD:main; then
              exit 0
            fi
          done
          echo 'Unable to push tested patch after safe retries.' >&2
          exit 1
