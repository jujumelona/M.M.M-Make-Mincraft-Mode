name: Build native llama CUDA SM75

on:
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: native-llama-sm75-b10375-cuda12.4-v2
  cancel-in-progress: false

jobs:
  build:
    uses: ./.github/workflows/build-native-llama-cuda.yml
    with:
      cuda_arch: "75"
    permissions:
      contents: write
