from __future__ import annotations

from pathlib import Path

ROOT = Path(__file__).resolve().parents[2]


def text(path: str) -> str:
    return (ROOT / path).read_text(encoding="utf-8")


def write(path: str, value: str) -> None:
    (ROOT / path).write_text(value, encoding="utf-8")


def replace_once(value: str, old: str, new: str, *, label: str) -> str:
    count = value.count(old)
    if count != 1:
        raise RuntimeError(f"{label}: expected one match, found {count}")
    return value.replace(old, new, 1)


def replace_between(value: str, start: str, end: str, replacement: str, *, label: str) -> str:
    start_i = value.find(start)
    if start_i < 0:
        raise RuntimeError(f"{label}: start marker missing")
    end_i = value.find(end, start_i)
    if end_i < 0:
        raise RuntimeError(f"{label}: end marker missing")
    return value[:start_i] + replacement + value[end_i:]


# ---------------------------------------------------------------------------
# 1. Model evidence projection: source bodies only, never retrieval envelopes.
# ---------------------------------------------------------------------------
path = "minecraft_mod_ai/agentic_pre_design_rag.py"
value = text(path)
trace_helper = '''\n\ndef _emit_rag_trace(event: str, **fields: Any) -> None:\n    print(\n        "PRE-DESIGN RAG TRACE: "\n        + json.dumps({"event": event, **fields}, ensure_ascii=False, sort_keys=True, default=str),\n        flush=True,\n    )\n'''
value = replace_once(
    value,
    "\n\ndef _research_page_messages(\n",
    trace_helper + "\n\ndef _research_page_messages(\n",
    label="insert model evidence trace helper",
)
new_evidence_units = '''def _evidence_units(evidence: Mapping[str, Any]):\n    """Project claim-bearing source bodies to the model; keep diagnostics host-only.\n\n    The raw evidence receipt remains losslessly persisted by\n    ``_materialize_domain_evidence_document``.  The model projection must never serialize\n    query/provider/fusion envelopes as evidence pages because those diagnostics can bury a\n    real source body across many 1.8KB fragments and make the model reason over retrieval\n    traces instead of source text.\n    """\n\n    for source_key, value in evidence.items():\n        if str(source_key) == "grounded_rag" and isinstance(value, Mapping):\n            queries = value.get("queries")\n            if isinstance(queries, list):\n                admitted = 0\n                for query_index, raw_query in enumerate(queries):\n                    if not isinstance(raw_query, Mapping):\n                        _emit_rag_trace(\n                            "evidence_query_skipped",\n                            source=str(source_key),\n                            query_index=query_index,\n                            reason="query_not_mapping",\n                        )\n                        continue\n                    query = str(raw_query.get("query") or "").strip()\n                    records = raw_query.get("evidence_records")\n                    if not isinstance(records, list) or not records:\n                        _emit_rag_trace(\n                            "evidence_query_no_source_body_records",\n                            source=str(source_key),\n                            query_index=query_index,\n                            query=query,\n                            provider_status=str(raw_query.get("github_provider_status") or ""),\n                            saturation_reason=str(raw_query.get("github_saturation_reason") or ""),\n                        )\n                        continue\n                    for record_index, raw_record in enumerate(records):\n                        if not isinstance(raw_record, Mapping):\n                            _emit_rag_trace(\n                                "evidence_record_skipped",\n                                source=str(source_key),\n                                query=query,\n                                query_index=query_index,\n                                record_index=record_index,\n                                reason="record_not_mapping",\n                            )\n                            continue\n                        body = str(\n                            raw_record.get("content")\n                            or raw_record.get("body")\n                            or raw_record.get("text")\n                            or ""\n                        ).strip()\n                        source_id = str(raw_record.get("source_id") or "").strip()\n                        if not body:\n                            _emit_rag_trace(\n                                "evidence_record_skipped",\n                                source=str(source_key),\n                                query=query,\n                                query_index=query_index,\n                                record_index=record_index,\n                                source_id=source_id,\n                                reason="no_source_body",\n                            )\n                            continue\n                        digest = str(raw_record.get("content_sha256") or "").strip() or _sha256_text(body)\n                        metadata = raw_record.get("metadata")\n                        safe_metadata: dict[str, Any] = {}\n                        if isinstance(metadata, Mapping):\n                            for key in ("repository", "default_branch", "readme_path", "path", "file_path"):\n                                item = metadata.get(key)\n                                if item not in (None, ""):\n                                    safe_metadata[key] = item\n                        projected = {\n                            "query": query,\n                            "source_id": source_id,\n                            "source_type": str(raw_record.get("source_type") or ""),\n                            "source_locator": str(raw_record.get("source_locator") or ""),\n                            "url": str(raw_record.get("url") or ""),\n                            "title": str(raw_record.get("title") or ""),\n                            "content_sha256": digest,\n                            "content": body,\n                            "retrieval_section": str(raw_record.get("retrieval_section") or ""),\n                            "evidence_origin": str(raw_record.get("evidence_origin") or ""),\n                            "metadata": safe_metadata,\n                        }\n                        admitted += 1\n                        _emit_rag_trace(\n                            "evidence_record_admitted",\n                            source=str(source_key),\n                            query=query,\n                            query_index=query_index,\n                            record_index=record_index,\n                            source_id=source_id,\n                            body_chars=len(body),\n                            body_sha256=digest,\n                        )\n                        yield f"{source_key}:source:{query_index}:{record_index}", projected\n                _emit_rag_trace(\n                    "grounded_rag_model_projection_complete",\n                    source=str(source_key),\n                    query_count=len(queries),\n                    source_body_unit_count=admitted,\n                    diagnostic_envelopes_excluded=True,\n                )\n                continue\n        yield str(source_key), value\n\n\n'''
value = replace_between(
    value,
    "def _evidence_units(evidence: Mapping[str, Any]):\n",
    "def _read_evidence_pages(document: Mapping[str, Any]) -> list[dict[str, Any]]:\n",
    new_evidence_units,
    label="replace evidence projection",
)
value = replace_once(
    value,
    '        "model_projection": "lossless_ordered_utf8_fragments",\n',
    '        "model_projection": "claim_bearing_source_bodies_only;raw_receipt_lossless",\n',
    label="update evidence projection receipt",
)
materialize_anchor = '''    _atomic_write_text(raw_path, raw_text)\n    _atomic_write_text(pages_path, pages_text)\n\n    return {\n'''
materialize_trace = '''    _atomic_write_text(raw_path, raw_text)\n    _atomic_write_text(pages_path, pages_text)\n    _emit_rag_trace(\n        "evidence_document_materialized",\n        domain_id=domain_id,\n        document_sha256=document_sha256,\n        raw_source_keys=sorted(str(key) for key in evidence),\n        model_unit_count=len(units),\n        page_count=page_count,\n        pages=[\n            {\n                "page_ref": str(page.get("page_ref") or ""),\n                "unit_id": str(page.get("unit_id") or ""),\n                "content_chars": len(str(page.get("content") or "")),\n                "content_sha256": _sha256_text(str(page.get("content") or "")),\n            }\n            for page in pages\n        ],\n    )\n\n    return {\n'''
value = replace_once(value, materialize_anchor, materialize_trace, label="trace materialized pages")
write(path, value)


# ---------------------------------------------------------------------------
# 2. Corrective loop: page-local model failures are recoverable, fully traced.
# ---------------------------------------------------------------------------
path = "minecraft_mod_ai/pre_design_rag_corrective.py"
value = text(path)
corrective_trace = '''\n\ndef _emit_corrective_trace(event: str, **fields: Any) -> None:\n    print(\n        "PRE-DESIGN RAG TRACE: "\n        + json.dumps({"event": event, **fields}, ensure_ascii=False, sort_keys=True, default=str),\n        flush=True,\n    )\n'''
value = replace_once(
    value,
    '_VERIFIED_FIXED_POINT = "verified_claims_sufficient"\n',
    '_VERIFIED_FIXED_POINT = "verified_claims_sufficient"\n' + corrective_trace,
    label="insert corrective trace helper",
)
page_loop_anchor = '''    for page_index, page in enumerate(pages):\n        page_ref = str(page.get("page_ref") or "").strip()\n        notes = project_rag._read_page_losslessly(\n'''
page_loop_new = '''    for page_index, page in enumerate(pages):\n        page_ref = str(page.get("page_ref") or "").strip()\n        page_content = str(page.get("content") or "")\n        failure_count_before = len(failures)\n        _emit_corrective_trace(\n            "page_read_start",\n            domain_id=domain_id,\n            round=round_index,\n            page_index=page_index,\n            page_ref=page_ref,\n            unit_id=str(page.get("unit_id") or ""),\n            content_chars=len(page_content),\n            content_sha256=project_rag._sha256_text(page_content),\n        )\n        notes = project_rag._read_page_losslessly(\n'''
value = replace_once(value, page_loop_anchor, page_loop_new, label="trace page read start")
claim_anchor = '''        candidates = _claim_candidates(\n            [note for note in notes if isinstance(note, Mapping)]\n        )\n        try:\n'''
claim_new = '''        candidates = _claim_candidates(\n            [note for note in notes if isinstance(note, Mapping)]\n        )\n        _emit_corrective_trace(\n            "page_claim_candidates",\n            domain_id=domain_id,\n            round=round_index,\n            page_index=page_index,\n            page_ref=page_ref,\n            note_count=len(notes),\n            candidate_count=len(candidates),\n            new_page_failures=len(failures) - failure_count_before,\n        )\n        try:\n'''
value = replace_once(value, claim_anchor, claim_new, label="trace claim candidates")
verify_except = '''        except Exception as exc:\n            failures.append(\n                {\n                    "unit": f"support:{round_index}:{page_index}",\n                    "error": f"{type(exc).__name__}: {exc}",\n                }\n            )\n            verified, rejected = [], candidates\n\n        rejected_count += len(rejected)\n'''
verify_new = '''        except Exception as exc:\n            failures.append(\n                {\n                    "unit": f"support:{round_index}:{page_index}",\n                    "error": f"{type(exc).__name__}: {exc}",\n                }\n            )\n            _emit_corrective_trace(\n                "page_support_verifier_failure",\n                domain_id=domain_id,\n                round=round_index,\n                page_index=page_index,\n                page_ref=page_ref,\n                exception_type=type(exc).__name__,\n                exception_message=str(exc),\n            )\n            verified, rejected = [], candidates\n\n        _emit_corrective_trace(\n            "page_support_result",\n            domain_id=domain_id,\n            round=round_index,\n            page_index=page_index,\n            page_ref=page_ref,\n            candidate_count=len(candidates),\n            verified_count=len(verified),\n            rejected_count=len(rejected),\n            total_recoverable_failures=len(failures),\n        )\n        rejected_count += len(rejected)\n'''
value = replace_once(value, verify_except, verify_new, label="trace support result")
while_anchor = '''        while round_index <= max_rounds:\n            pages, notes, rejected = _read_and_verify_document(\n'''
while_new = '''        while round_index <= max_rounds:\n            _emit_corrective_trace(\n                "corrective_round_start",\n                domain_id=domain_id,\n                round=round_index,\n                max_rounds=max_rounds,\n                document_sha256=str(documents[-1].get("document_sha256") or ""),\n                searched_query_count=len(searched),\n                recoverable_failure_count=len(failures),\n            )\n            pages, notes, rejected = _read_and_verify_document(\n'''
value = replace_once(value, while_anchor, while_new, label="trace corrective round")
poison_block = '''            if failures:\n                fixed_point = "bounded_extraction_or_support_verification_failure"\n                break\n            if active_summary.get("claims") and not active_gaps:\n'''
poison_new = '''            # Page-local model/JSON/support failures are quarantined to that page.  They\n            # must never erase independently verified claims from other source-body pages\n            # or prevent corrective retrieval from trying stronger evidence.\n            if active_summary.get("claims") and not active_gaps:\n'''
value = replace_once(value, poison_block, poison_new, label="remove page failure poison")
reason_old = '''        if failures:\n            reasons.append("bounded extraction/support verification failure")\n        if not claims:\n'''
reason_new = '''        if failures and fixed_point != _VERIFIED_FIXED_POINT:\n            reasons.append("bounded extraction/support verification failure")\n        if not claims:\n'''
value = replace_once(value, reason_old, reason_new, label="terminal failure semantics")
fail_note_old = '''        if failures:\n            note["research_failures"] = list(failures)\n            note["gaps"] = [\n                *list(note.get("gaps", ())),\n                *(f"{item['unit']}: {item['error']}" for item in failures),\n            ]\n        if reasons:\n'''
fail_note_new = '''        if failures and reasons:\n            note["research_failures"] = list(failures)\n            note["gaps"] = [\n                *list(note.get("gaps", ())),\n                *(f"{item['unit']}: {item['error']}" for item in failures),\n            ]\n        elif failures:\n            note["research_warnings"] = list(failures)\n        _emit_corrective_trace(\n            "corrective_terminal_decision",\n            domain_id=domain_id,\n            fixed_point=fixed_point,\n            claim_count=len(claims),\n            active_gap_count=len(summary.get("gaps") or ()),\n            recoverable_failure_count=len(failures),\n            reasons=reasons,\n        )\n        if reasons:\n'''
value = replace_once(value, fail_note_old, fail_note_new, label="quarantine recoverable failures")
write(path, value)


# ---------------------------------------------------------------------------
# 3. Claim support: log each acceptance/rejection reason.
# ---------------------------------------------------------------------------
path = "minecraft_mod_ai/pre_design_rag_support.py"
value = text(path)
support_trace = '''\n\ndef _emit_support_trace(event: str, **fields: Any) -> None:\n    print(\n        "PRE-DESIGN RAG TRACE: "\n        + json.dumps({"event": event, **fields}, ensure_ascii=False, sort_keys=True, default=str),\n        flush=True,\n    )\n'''
value = replace_once(value, "_MIN_QUOTE_CHARS = 8\n", "_MIN_QUOTE_CHARS = 8\n" + support_trace, label="insert support trace")
value = replace_once(
    value,
    '''            if not supported:\n                rejected.append(claim)\n                continue\n            if (\n                not quote\n                or quote not in content\n                or len("".join(quote.split())) < _MIN_QUOTE_CHARS\n            ):\n                rejected.append(claim)\n                continue\n            accepted.append(\n''',
    '''            if not supported:\n                _emit_support_trace(\n                    "claim_support_rejected",\n                    page_ref=page_ref,\n                    claim_index=index,\n                    claim=claim,\n                    reason="model_marked_unsupported",\n                )\n                rejected.append(claim)\n                continue\n            if (\n                not quote\n                or quote not in content\n                or len("".join(quote.split())) < _MIN_QUOTE_CHARS\n            ):\n                _emit_support_trace(\n                    "claim_support_rejected",\n                    page_ref=page_ref,\n                    claim_index=index,\n                    claim=claim,\n                    reason=(\n                        "missing_quote"\n                        if not quote\n                        else "quote_not_exact_page_substring"\n                        if quote not in content\n                        else "quote_too_short"\n                    ),\n                    quote_chars=len(quote),\n                )\n                rejected.append(claim)\n                continue\n            _emit_support_trace(\n                "claim_support_accepted",\n                page_ref=page_ref,\n                claim_index=index,\n                claim=claim,\n                quote_chars=len(quote),\n                quote_sha256=_sha256_text(quote),\n            )\n            accepted.append(\n''',
    label="trace support verdict reasons",
)
write(path, value)


# ---------------------------------------------------------------------------
# 4. GitHub acquisition: deterministic query plan, unauth rate budget, full receipts.
# ---------------------------------------------------------------------------
path = "minecraft_mod_ai/pre_design_external_source_contract.py"
value = text(path)
value = replace_once(value, "import hashlib\n", "import hashlib\nimport json\n", label="import json for source trace")
value = replace_once(
    value,
    '''def _max_queries() -> int:\n    raw = os.environ.get("MMM_PREDESIGN_EXTERNAL_SOURCE_QUERIES", "").strip()\n    try:\n        value = int(raw) if raw else _DEFAULT_MAX_QUERIES\n    except ValueError:\n        value = _DEFAULT_MAX_QUERIES\n    return max(1, min(value, _HARD_MAX_QUERIES))\n''',
    '''def _github_token() -> str:\n    return os.environ.get("GITHUB_TOKEN", "").strip() or os.environ.get("GH_TOKEN", "").strip()\n\n\ndef _max_queries() -> int:\n    raw = os.environ.get("MMM_PREDESIGN_EXTERNAL_SOURCE_QUERIES", "").strip()\n    try:\n        value = int(raw) if raw else _DEFAULT_MAX_QUERIES\n    except ValueError:\n        value = _DEFAULT_MAX_QUERIES\n    # GitHub unauthenticated repository search is only 10 requests/minute.  Never\n    # configure the pre-design phase to deterministically exceed that ceiling itself;\n    # leave two requests of headroom for other process activity.\n    provider_cap = _HARD_MAX_QUERIES if _github_token() else 8\n    return max(1, min(value, _HARD_MAX_QUERIES, provider_cap))\n''',
    label="bound unauthenticated query budget",
)
value = replace_once(
    value,
    '''def _clean_query(value: Any) -> str:\n    return " ".join(str(value or "").split()).strip()\n''',
    '''def _clean_query(value: Any) -> str:\n    return " ".join(str(value or "").split()).strip()\n\n\ndef _emit_source_trace(event: str, **fields: Any) -> None:\n    print(\n        "PRE-DESIGN RAG TRACE: "\n        + json.dumps({"event": event, **fields}, ensure_ascii=False, sort_keys=True, default=str),\n        flush=True,\n    )\n\n\ndef _repository_search_query(query: str) -> str:\n    # Repository search is AND-like.  Feeding the full natural-language retrieval query\n    # over-constrains discovery.  Keep only the first two domain-specific terms plus\n    # minecraft, while preserving the original query in every receipt.\n    ordered: list[str] = []\n    for token in _WORD.findall(query):\n        folded = token.casefold()\n        if len(folded) < 3 or folded in _STOP or folded in ordered:\n            continue\n        ordered.append(folded)\n    compact = ordered[:2]\n    if "minecraft" not in compact:\n        compact.append("minecraft")\n    return " ".join(compact) or "minecraft"\n''',
    label="insert source acquisition trace helpers",
)
value = replace_once(
    value,
    '''    token = os.environ.get("GITHUB_TOKEN", "").strip()\n    if token:\n''',
    '''    token = _github_token()\n    if token:\n''',
    label="use github token fallback",
)
new_retrieve = '''def _retrieve_github_source_body(query: str) -> dict[str, Any]:\n    """Search GitHub and return at most one verified README body for *query*."""\n\n    import httpx\n\n    search_requests = 0\n    source_requests = 0\n    errors: list[str] = []\n    records: list[dict[str, Any]] = []\n    headers = _headers()\n    search_query = _repository_search_query(query)\n    _emit_source_trace(\n        "github_query_start",\n        query=query,\n        repository_search_query=search_query,\n        authenticated=bool(_github_token()),\n        max_repository_candidates=_MAX_REPOSITORIES_PER_QUERY,\n    )\n\n    try:\n        with httpx.Client(timeout=20.0, follow_redirects=True, headers=headers) as client:\n            search_requests += 1\n            response = client.get(\n                f"{_GITHUB_API}/search/repositories",\n                params={\n                    "q": search_query + " in:name,description,readme,topics",\n                    "per_page": _MAX_REPOSITORIES_PER_QUERY,\n                },\n            )\n            remaining = str(response.headers.get("x-ratelimit-remaining", ""))\n            reset = str(response.headers.get("x-ratelimit-reset", ""))\n            _emit_source_trace(\n                "github_search_response",\n                query=query,\n                repository_search_query=search_query,\n                http_status=response.status_code,\n                rate_remaining=remaining,\n                rate_reset=reset,\n            )\n            if response.status_code != 200:\n                error = _status_error(response, operation="repository search")\n                errors.append(error)\n                status = "rate_limited" if response.status_code in {403, 429} else "unavailable"\n                _emit_source_trace(\n                    "github_search_failed",\n                    query=query,\n                    error=error,\n                    provider_status=status,\n                )\n                return {\n                    "records": records,\n                    "search_requests": search_requests,\n                    "source_requests": source_requests,\n                    "provider_status": status,\n                    "saturation_reason": "provider_limited" if status == "rate_limited" else "search_failed",\n                    "errors": errors,\n                }\n            try:\n                payload = response.json()\n            except Exception as exc:\n                error = f"github repository search JSON decode failed: {type(exc).__name__}: {exc}"\n                errors.append(error)\n                _emit_source_trace("github_search_decode_failed", query=query, error=error)\n                return {\n                    "records": records,\n                    "search_requests": search_requests,\n                    "source_requests": source_requests,\n                    "provider_status": "unavailable",\n                    "saturation_reason": "search_response_invalid",\n                    "errors": errors,\n                }\n            items = payload.get("items") if isinstance(payload, Mapping) else None\n            repositories = [item for item in (items or ()) if isinstance(item, Mapping)]\n            _emit_source_trace(\n                "github_search_candidates",\n                query=query,\n                candidate_count=len(repositories),\n                total_count=(payload.get("total_count") if isinstance(payload, Mapping) else None),\n                candidates=[str(item.get("full_name") or "") for item in repositories[:_MAX_REPOSITORIES_PER_QUERY]],\n            )\n            if not repositories:\n                return {\n                    "records": records,\n                    "search_requests": search_requests,\n                    "source_requests": source_requests,\n                    "provider_status": "available",\n                    "saturation_reason": "search_exhausted_no_repository",\n                    "errors": errors,\n                }\n\n            for candidate_index, repository in enumerate(repositories[:_MAX_REPOSITORIES_PER_QUERY]):\n                full_name = str(repository.get("full_name") or "").strip()\n                if not full_name or "/" not in full_name:\n                    _emit_source_trace(\n                        "github_repository_skipped",\n                        query=query,\n                        candidate_index=candidate_index,\n                        reason="invalid_full_name",\n                    )\n                    continue\n                _emit_source_trace(\n                    "github_repository_selected_for_body",\n                    query=query,\n                    candidate_index=candidate_index,\n                    repository=full_name,\n                )\n                source_requests += 1\n                readme = client.get(f"{_GITHUB_API}/repos/{full_name}/readme")\n                _emit_source_trace(\n                    "github_readme_response",\n                    query=query,\n                    repository=full_name,\n                    http_status=readme.status_code,\n                    rate_remaining=str(readme.headers.get("x-ratelimit-remaining", "")),\n                    rate_reset=str(readme.headers.get("x-ratelimit-reset", "")),\n                )\n                if readme.status_code != 200:\n                    if readme.status_code not in {404}:\n                        errors.append(_status_error(readme, operation=f"README fetch {full_name}"))\n                    _emit_source_trace(\n                        "github_readme_rejected",\n                        query=query,\n                        repository=full_name,\n                        reason="http_not_200",\n                        http_status=readme.status_code,\n                    )\n                    continue\n                try:\n                    readme_payload = readme.json()\n                except Exception as exc:\n                    error = f"github README JSON decode failed for {full_name}: {type(exc).__name__}: {exc}"\n                    errors.append(error)\n                    _emit_source_trace("github_readme_rejected", query=query, repository=full_name, reason="json_decode_failed", error=error)\n                    continue\n                if not isinstance(readme_payload, Mapping):\n                    _emit_source_trace("github_readme_rejected", query=query, repository=full_name, reason="payload_not_mapping")\n                    continue\n                body = _decode_readme(readme_payload)\n                if not body:\n                    _emit_source_trace("github_readme_rejected", query=query, repository=full_name, reason="empty_or_too_short_body")\n                    continue\n                if not _body_relevant(query, body):\n                    _emit_source_trace(\n                        "github_readme_rejected",\n                        query=query,\n                        repository=full_name,\n                        reason="body_not_query_relevant",\n                        body_chars=len(body),\n                        body_sha256="sha256:" + hashlib.sha256(body.encode("utf-8")).hexdigest(),\n                    )\n                    continue\n                locator = str(readme_payload.get("html_url") or repository.get("html_url") or "").strip()\n                if not locator:\n                    _emit_source_trace("github_readme_rejected", query=query, repository=full_name, reason="missing_locator")\n                    continue\n                digest = "sha256:" + hashlib.sha256(body.encode("utf-8")).hexdigest()\n                record = {\n                    "source_id": "github:" + full_name + ":" + str(readme_payload.get("sha") or digest.removeprefix("sha256:")),\n                    "source_type": "github_source_body",\n                    "source_locator": locator,\n                    "url": locator,\n                    "title": full_name,\n                    "content": body,\n                    "content_sha256": digest,\n                    "body_retrieved": True,\n                    "metadata": {\n                        "repository": full_name,\n                        "default_branch": str(repository.get("default_branch") or ""),\n                        "readme_path": str(readme_payload.get("path") or "README"),\n                        "query": query,\n                    },\n                    "retrieval": {\n                        "provider": "github",\n                        "provider_status": "available",\n                        "body_retrieved": True,\n                    },\n                }\n                records.append(record)\n                _emit_source_trace(\n                    "github_source_body_admitted",\n                    query=query,\n                    repository=full_name,\n                    source_id=record["source_id"],\n                    locator=locator,\n                    body_chars=len(body),\n                    body_sha256=digest,\n                )\n                break\n    except Exception as exc:\n        error = f"github source acquisition failed: {type(exc).__name__}: {exc}"\n        errors.append(error)\n        _emit_source_trace("github_transport_failure", query=query, error=error)\n        return {\n            "records": records,\n            "search_requests": search_requests,\n            "source_requests": source_requests,\n            "provider_status": "unavailable",\n            "saturation_reason": "transport_failure",\n            "errors": errors,\n        }\n\n    saturation = "source_body_retrieved" if records else "repositories_found_no_claim_bearing_source_body"\n    _emit_source_trace(\n        "github_query_complete",\n        query=query,\n        search_requests=search_requests,\n        source_requests=source_requests,\n        source_body_count=len(records),\n        saturation_reason=saturation,\n        errors=errors,\n    )\n    return {\n        "records": records,\n        "search_requests": search_requests,\n        "source_requests": source_requests,\n        "provider_status": "available",\n        "saturation_reason": saturation,\n        "errors": errors,\n    }\n\n\n'''
value = replace_between(value, "def _retrieve_github_source_body(query: str) -> dict[str, Any]:\n", "def _stable_queries(value: Any) -> list[str]:\n", new_retrieve, label="replace github retrieval")
# Trace selection/skip and stop hammering once the provider is rate limited.
value = replace_once(
    value,
    '''    limit = _max_queries()\n    selected = _planned_requirement_query_keys(payload)\n    if not selected:\n        selected = _fallback_query_keys(bundle, limit)\n    selected = set(list(selected)[:limit])\n\n    augmented_domains: list[Any] = []\n''',
    '''    limit = _max_queries()\n    selected = _planned_requirement_query_keys(payload)\n    selection_origin = "approved_requirement_queries"\n    if not selected:\n        selected = _fallback_query_keys(bundle, limit)\n        selection_origin = "bounded_fallback_queries"\n    selected = set(list(selected)[:limit])\n    provider_rate_limited = False\n    _emit_source_trace(\n        "external_query_plan",\n        selection_origin=selection_origin,\n        configured_limit=limit,\n        authenticated=bool(_github_token()),\n        selected_count=len(selected),\n        selected_queries=sorted(selected),\n    )\n\n    augmented_domains: list[Any] = []\n''',
    label="trace external query selection",
)
value = replace_once(
    value,
    '''            query = _clean_query(row.get("query"))\n            if query.casefold() in selected:\n                receipt = _retrieve_github_source_body(query)\n                external = row.get("external_rag")\n''',
    '''            query = _clean_query(row.get("query"))\n            if query.casefold() in selected:\n                if provider_rate_limited:\n                    receipt = {\n                        "records": [],\n                        "search_requests": 0,\n                        "source_requests": 0,\n                        "provider_status": "rate_limited",\n                        "saturation_reason": "skipped_after_provider_rate_limit",\n                        "errors": ["github provider already reported rate limit in this pre-design pass"],\n                    }\n                    _emit_source_trace(\n                        "external_query_skipped",\n                        query=query,\n                        reason="provider_already_rate_limited",\n                    )\n                else:\n                    _emit_source_trace("external_query_selected", query=query, reason=selection_origin)\n                    receipt = _retrieve_github_source_body(query)\n                    if str(receipt.get("provider_status") or "").casefold() == "rate_limited":\n                        provider_rate_limited = True\n                external = row.get("external_rag")\n''',
    label="trace selected query and stop after rate limit",
)
value = replace_once(
    value,
    '''                row["external_rag"] = external_map\n            rows.append(row)\n''',
    '''                row["external_rag"] = external_map\n            else:\n                _emit_source_trace(\n                    "external_query_skipped",\n                    query=query,\n                    reason="not_selected_by_bounded_query_plan",\n                )\n            rows.append(row)\n''',
    label="trace unselected queries",
)
write(path, value)


# ---------------------------------------------------------------------------
# Regression: reproduce the exact runtime failure class, not just counters.
# ---------------------------------------------------------------------------
test_path = ROOT / "tests/test_predesign_rag_runtime_root_cause.py"
test_path.write_text('''from __future__ import annotations\n\nfrom pathlib import Path\n\nimport minecraft_mod_ai.agentic_pre_design_rag as rag\nimport minecraft_mod_ai.pre_design_external_source_contract as external\n\n\ndef _grounded(body: str) -> dict[str, object]:\n    return {\n        "grounded_rag": {\n            "domain_id": "request",\n            "fusion": {"diagnostic": "SHOULD_NOT_REACH_MODEL"},\n            "queries": [\n                {\n                    "query": "minecraft mod travel space",\n                    "github_provider_status": "available",\n                    "github_saturation_reason": "source_body_retrieved",\n                    "evidence_records": [\n                        {\n                            "source_id": "github:fixture/repo:abc",\n                            "source_type": "github_source_body",\n                            "source_locator": "https://example.invalid/repo",\n                            "url": "https://example.invalid/repo",\n                            "title": "fixture/repo",\n                            "content": body,\n                            "content_sha256": rag._sha256_text(body),\n                            "metadata": {"repository": "fixture/repo", "query": "metadata-only"},\n                        }\n                    ],\n                    "retrieval_errors": [],\n                }\n            ],\n        }\n    }\n\n\ndef test_model_evidence_projection_contains_source_body_not_retrieval_envelope(tmp_path, monkeypatch) -> None:\n    body = "REAL SOURCE BODY describing space travel mechanics and ship progression. " * 50\n    monkeypatch.setenv("MMM_WORKSPACE", str(tmp_path))\n    document = rag._materialize_domain_evidence_document("request", _grounded(body))\n    pages = rag._read_evidence_pages(document)\n    joined = "\\n".join(str(page["content"]) for page in pages)\n    assert "REAL SOURCE BODY" in joined\n    assert "SHOULD_NOT_REACH_MODEL" not in joined\n    assert "github_provider_status" not in joined\n    assert "github_saturation_reason" not in joined\n    assert "retrieval_errors" not in joined\n    assert document["model_projection"] == "claim_bearing_source_bodies_only;raw_receipt_lossless"\n\n\ndef test_unauthenticated_external_query_budget_stays_below_github_search_ceiling(monkeypatch) -> None:\n    monkeypatch.delenv("GITHUB_TOKEN", raising=False)\n    monkeypatch.delenv("GH_TOKEN", raising=False)\n    monkeypatch.setenv("MMM_PREDESIGN_EXTERNAL_SOURCE_QUERIES", "20")\n    assert external._max_queries() == 8\n\n\ndef test_repository_search_query_is_bounded_and_not_full_natural_language() -> None:\n    value = external._repository_search_query(\n        "minecraft mod gather resources space environment with a very long tail"\n    )\n    assert value == "gather resources minecraft"\n    assert "environment" not in value\n    assert "very long tail" not in value\n''', encoding="utf-8")

# Self-remove the one-shot staging files so the repair commit leaves only production/tests.
for temporary in (
    ROOT / ".github/scripts/one_shot_rag_runtime_root_repair.py",
    ROOT / ".github/workflows/one-shot-rag-runtime-root-repair.yml",
):
    if temporary.exists():
        temporary.unlink()
